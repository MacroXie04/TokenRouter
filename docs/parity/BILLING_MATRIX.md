# Billing parity matrix

Fresh audit: 2026-09-05. Target revision: `a88784d16269d86f1c37298b5a185e6cfe7fccd0` plus the audited working-tree changes. Read-only reference revision: `/Users/hongzhe/Code/new-api@ccd535ef8e50cf6e5846a59278c40b7ff59d1b7d`.

This is a boundary matrix, not a feature-name checklist. `PASS` means a deterministic executable test reaches the stated success and failure boundary. `IN_PROGRESS` means useful implementation exists but a reference behavior or deterministic proof is incomplete. `NOT_STARTED` means the reference behavior has no operative target implementation. `BLOCKED_EXTERNAL` is reserved here for a live third-party check that needs an unavailable payment-provider environment; local mocks and signature fixtures are recorded separately and are not treated as live proof.

Status totals: **98 PASS / 0 IN_PROGRESS / 0 NOT_STARTED / 3 BLOCKED_EXTERNAL (101 rows total)**.

Every exact `Test*` name cited by an evidence row resolves to a declaration in
the current tree; the mechanical check is recorded below.

Most deterministic database evidence below uses isolated SQLite fixtures. Row A-07 separately records the complete transaction suite that passed on isolated MySQL and PostgreSQL 17. Neither local nor external-database evidence is presented as a live payment-gateway exchange; those gaps remain `BLOCKED_EXTERNAL` in W-20 through W-22.

## Pricing and quota arithmetic

| ID | Billing boundary | Status | Deterministic evidence or exact gap |
|---|---|---|---|
| P-01 | Flat prompt and completion token prices | PASS | `service.ComputeQuotaChecked` prices prompt and completion tokens independently in USD per million. `TestComputeQuotaDeterministic` checks the exact conversion. |
| P-02 | Unconfigured-model flat-price fallback | PASS | The target deliberately falls back to USD 1/M prompt and USD 3/M completion. `TestComputeBillingQuotaFlatFallback` proves the executable fallback. This is target behavior, not proof of the reference's configurable ratio fallback. |
| P-03 | Selected-group price multiplier | PASS | `service.GroupRatio` is applied by both flat and expression pricing. `TestGroupRatioAffectsQuota` proves the exact multiplier. |
| P-04 | Pricing registry isolation, concurrent reads, persistence and hot publication | PASS | `TestPricingRegistriesUseDefensiveCopies`, `TestPricingRegistriesConcurrentAccess`, and `TestPricingOptionsWriteThroughToLiveBilling` cover defensive copies, concurrent replacement/read, validation, persistence, and immediate billing visibility. |
| P-05 | Tiered expression dispatch, tier selection and version prefix | PASS | `TestComputeBillingQuotaTiered`, `TestTierSelection`, and `TestVersionPrefixStripped` execute the production service dispatch and expression engine. |
| P-06 | Cache-read (`cr`) differentiated pricing | PASS | `TestComputeBillingQuotaTieredWithCache` reaches `service.ComputeBillingQuota`; `TestFlatPricingExcludesCacheRead` proves that separately priced OpenAI cache-read tokens are subtracted from base prompt tokens. |
| P-07 | General cache-creation (`cc`) differentiated pricing | PASS | `service.usageToBilling` maps generic cache-miss/write and Claude five-minute cache-creation counters; `TestUsageToBilling` and `TestComputeBillingQuotaTieredClaudeCacheTTLBreakdown` prove that `cc` reaches the production expression calculation. |
| P-08 | Claude 1-hour cache creation (`cc1h`) | PASS | `protocolkit.ClaudeUsage` preserves the five-minute/one-hour breakdown, `service.usageToBilling` maps it, and `TestComputeBillingQuotaTieredClaudeCacheTTLBreakdown` proves distinct `cc` and `cc1h` prices in the production path. |
| P-09 | Image/audio input and output differentiated pricing | PASS | `TestOrdinaryRelayTieredMultimodalBucketsPriceIndependently` drives the production relay settlement separately for `img`, `img_o`, `ai`, and `ao`, proves opt-in exclusion without double counting, and keeps ordinary `p`/`c` pricing unchanged. `TestOrdinaryRelayTieredMultimodalUsageSettlesExactAccounting` proves a 512-quota wallet/token reservation settles to the manually derived 279 charge, returns the unused 233 hold to both balances, and writes exactly one matching consume log and terminal durable reservation. `TestGeminiNativeTieredMultimodalUsageSettlesExactAccounting` and `TestOpenAIImagesUsageAliasesTieredMultimodalSettlement` reach the same exact charge from native Gemini modality metadata and OpenAI `input_tokens`/`output_tokens` aliases through real HTTP adapters. `TestNormalizeOpenAIUsageAliasesUsesEachBucketOnce`, `TestGeminiUsageToOpenAIUsageFailsClosedOnOverflow`, `TestSubcategoryNamesInsideStringsDoNotChangeTokenNormalization`, `TestSeparatelyPricedBucketsClampInconsistentBaseTotals`, and `TestOrdinaryRelayRejectsOversizedUsageWithoutRefundingAcceptedWork` cover alias duplication, aggregation overflow, false variable detection, inconsistent detail totals, count bounds, and accepted-work hold retention. |
| P-10 | Request parameter and header expression inputs | PASS | `TestParamFunction` and `TestHeaderFunction` execute the expression helpers with decoded request input. |
| P-11 | Time-window expression helpers | PASS | `pkg/billingexpr` uses an injectable clock and explicit requested timezone; `TestTimeWindowHelpersUseRequestedTimezoneZone` fixes the instant and proves hour/minute/weekday/month/day deterministically. |
| P-12 | Invalid configured expression behavior | PASS | `TestInvalidExpression` proves compiler rejection. `TestComputeBillingQuotaRejectsInvalidTieredExpressionAtomically` proves an invalid combined mode/expression update persists neither key and cannot replace the last valid price state; subsequent production billing therefore uses the unchanged flat registry rather than silently publishing a broken tiered configuration. |
| P-13 | Per-call price and duration-unit multiplication | PASS | `TestComputePerCallQuotaAndTokenReservation` proves USD-per-call conversion, group multiplication, zero price, invalid units, overflow rejection, and multiple units; Jimeng frame-duration selection is covered again in R-14. |
| P-14 | NaN and infinity handling | PASS | `TestQuotaFromFloatNaNInf` and `TestQuotaConversionsHandleExtremeUncheckedInputs` prove non-finite values cannot wrap into an unsafe debit/credit. |
| P-15 | Machine-width and persisted-domain overflow | PASS | `TestQuotaFromFloatSaturation`, `TestQuotaMathNeverNegativeOnOverflow`, and `TestAddQuotaWithinBoundsRejectsMachineAndDomainOverflow` prove saturation and bounded addition across 32/64-bit inputs. |
| P-16 | Exact-decimal quota conversion | PASS | `TestQuotaFromDecimalStrict` and `TestCostToQuotaNeverNegativeOnOverflow` prove strict failure for out-of-domain decimal results and non-negative expression conversion. |
| P-17 | Reference fixed-price, `ModelRatio`, and `CompletionRatio` semantics | PASS | Ordinary relays can explicitly select `ModelBillingMode: "reference"`; this compatibility namespace preserves the target's intentional prompt/completion-object `ModelPrice` contract by reading the reference numeric fixed-price map from `PerCallModelPrice`, while leaving USD-per-million, tiered-expression, Realtime, and asynchronous task billing unchanged. A configured fixed price—including zero—wins; otherwise `ModelRatio` is required, the pre-dispatch hold truncates `(max(prompt, PreConsumedQuota) + explicit nonzero max tokens) * ModelRatio * effective group ratio` without applying `CompletionRatio`, and final billable usage rounds `(prompt + completion * CompletionRatio) * ModelRatio * effective group ratio`. Completion-family defaults, locked ratios, provider-qualified overrides, special-name normalization, zero values, fixed-price token independence, the ratio one-quota minimum, and exact user-group-to-target-group overrides match the audited reference behavior. Each request snapshots the validated pricing/group inputs before dispatch, so hot reloads and retries cannot change its settlement or log fields; missing/invalid configuration and out-of-domain arithmetic fail before dispatch or settlement. `TestReferencePricingFixedPriceModelRatioAndCompletionRatioSemantics`, `TestReferencePricingCompletionFamilyDefaultsLocksAndQualifiedOverride`, `TestReferencePricingValidationDefaultsAndFailClosedOverflow`, `TestReferencePricingHotReloadKeepsInFlightSnapshotAndPublishesCoherently`, `TestReferencePricingConcurrentResolutionUsesOnlyCoherentSnapshots`, `TestOrdinaryRelayReferenceFixedPriceUsesSnapshotAndWritesExactAccounting`, `TestReferencePricingLogNeverMixesHotReloadedGroupRatios`, `TestOrdinaryRelayReferenceRatioFinalCorrectionRefundShortfallAndStreamingFallback`, `TestOrdinaryRelayReferenceFixedPriceFailureRefundsBothHolds`, `TestOrdinaryRelayReferenceZeroFixedPriceSettlesDurablyWithoutACharge`, `TestOrdinaryRelayReferenceMissingPricingFailsBeforeDispatch`, and `TestOrdinaryRelayReferenceFixedPriceConcurrentReservationsCannotOverspend` cover the boundary. |
| P-18 | Reference user-group to target-group special ratio | PASS | A validated, hot-reloadable `GroupGroupRatio` registry selects an explicit `(user group, using group)` override—including a deliberate zero-price override—before falling back to the using group's base ratio. The authenticated user group remains distinct from the selected routing group, and the exact effective ratio is used for worst-case holds, flat/tiered settlement, Realtime, per-call Jimeng billing, pricing/group discovery, and durable audit metadata. `TestUserGroupSpecialRatioPrecedenceAcrossBillingModes`, `TestGroupGroupRatioOptionValidationAndReload`, `TestGroupGroupRatioRegistryUsesDefensiveCopies`, `TestGroupGroupRatioRegistryConcurrentAccess`, `TestOrdinaryRelayUsesUserGroupSpecialRatioForHoldSettlementAndLog`, `TestProductionRealtimePricingUsesUserGroupSpecialRatio`, and `TestJimengOfficialTaskContract` cover precedence, validation/publication, concurrency, reservations, settlement and asynchronous persistence. |
| P-19 | Reference `TrustQuota` pre-consume bypass | PASS | Ordinary wallet-funded relays skip both wallet and limited-token holds only when the live user balance and live limited-token balance are each strictly greater than the immutable `10 * QuotaPerUnit`; an unlimited token satisfies the token side, while subscription funding and forced reservation callers (Realtime/asynchronous work) always retain their full holds. The zero-hold decision is an explicit durable `trust_quota_bypassed` invariant, dispatch retains the requested estimate as its crash-safe fallback, and final usage still atomically charges user/token/channel counters or remains durably retryable without negative balances. `TestOrdinaryRelayTrustQuotaStrictThresholdAndFundingPolicy`, `TestOrdinaryRelayTrustQuotaFinalChargeRefundAndShortfall`, `TestOrdinaryRelayTrustQuotaInsufficientFinalBalanceIsAtomicAndRecoverable`, `TestOrdinaryRelayTrustQuotaConcurrentSettlementCannotOverspend`, `TestOrdinaryRelayTrustQuotaSettlementOverflowFailsClosed`, `TestOrdinaryRelayTrustQuotaZeroHoldRequiresDurableMarker`, `TestRelayTrustQuotaIgnoresMutableCompatibilityOption`, `TestOrdinaryRelayTrustQuotaUsesDurableZeroHoldAndSettlesActual`, `TestOrdinaryRelayTrustQuotaRetryFailureRefundsZeroHoldExactlyOnce`, and `TestRelayQuotaTrustMarkerFreshAndLegacyMigration` cover strict boundaries, funding/token variants, ordinary-route wiring, final charge/refund/shortfall, concurrent requests, insufficient-balance recovery, failure/retry behavior, immutable configuration, and fresh/legacy schema defaults. |
| P-20 | Tool-price defaults, operator overrides, model-prefix precedence, explicit zero, and atomic publication | PASS | Tool prices are bounded USD-per-1,000-call decimals. Built-in defaults remain separate from the persisted override object; the longest matching `tool:model-prefix*` rule wins and an explicit zero is terminal. Canonical lexical JSON, duplicate/hostile exponent/size/cardinality/coefficient rejection, immutable snapshots, concurrent readers, and all-or-nothing publication across pricing domains are covered by `TestToolPriceDefaultsAndModelOverrides`, `TestToolPriceLongestPrefixAndExplicitZeroAreTerminal`, `TestToolPriceCanonicalJSONIsLexicalAndDetached`, `TestToolPriceStrictParserRejectsMalformedAndAmbiguousConfiguration`, `TestToolPriceParserBoundsSizeCardinalityAndCoefficient`, `TestToolPriceHostileExponentsRejectBeforeDecimalOperations`, `TestToolPriceAtomicReplacementPreservesOldSnapshotsAndRejectsInvalid`, `TestToolPriceConcurrentSnapshotReadsRemainCoherent`, and `TestPricingReliabilityAndToolPublicationIsAtomic`. |
| P-21 | Request-stable cross-protocol tool observation, identity deduplication, and retry reset | PASS | A request freezes its pricing snapshot before dispatch. Responses' stable IDs and index aliases, Chat tool-call IDs and index fallbacks, Claude custom tools versus cumulative server-tool counts, and terminal image outputs use separate bounded observation paths; retries discard the prior attempt's observations without changing prices. `TestToolPricingContextAndModeAreExplicit`, `TestResponsesToolCallsDeduplicateReplayAndSeparateCustomFromBuiltIn`, `TestToolCounterKeepsRequestStartSnapshotAcrossReload`, `TestChatStreamStableIDAndIndexFallbackDeduplicate`, `TestClaudeCustomAndServerToolCountsUseIndependentPaths`, `TestResponsesImageGenerationDedupCapAndTerminalReset`, and `TestToolCounterResetForRetryClearsAttemptButPreservesSnapshot` cover mode separation, ambiguity rejection, replay, terminal state, and hot reload. |
| P-22 | Exact tool-surcharge arithmetic, effective group ratio, audit shape, and safety bounds | PASS | Completed priced calls are summed with exact decimal arithmetic, multiplied by the effective group ratio and immutable quota unit, rounded once, sorted deterministically for audit, and frozen after settlement. Explicit-zero/free-group behavior remains auditable without a debit; invalid ratios, unsafe names/counts, observation caps, and quota overflow fail closed. `TestToolSettlementExactArithmeticRoundingSortingAndZeroGroup`, `TestToolSettlementFailsClosedOnInvalidRatioAndOverflow`, `TestToolSettlementAuditIsDetachedAndReferenceCompatible`, `TestToolObservationAndCountCaps`, `TestToolObservationRejectsUnsafeNamesAndOversizedReportedCount`, `TestZeroPricePrefixDisablesCustomCallAtObservation`, and `TestToolQuotaUsesImmutableAccountingUnit` cover the boundary. |
| P-23 | Built-in search/image markers and alpha-search compatibility | PASS | Responses bills an observed built-in execution rather than a declaration, distinguishes `web_search` from `web_search_preview`, and counts only completed image generations. Gemini native search and a successful alpha-search mark one protocol-defined tool execution, while legacy `*-search-preview` inference is confined to Chat Completions and an explicit zero remains non-billable. `TestResponsesWebSearchUsesDeclaredBuiltInVariantWithoutBillingDeclarations`, `TestExplicitZeroBuiltInIsObservedButNeverBilled`, `TestNativeMarkersAndLegacySearchPreviewSemantics`, `TestResponsesImageGenerationDedupCapAndTerminalReset`, `TestRelayAlphaSearchPassthrough`, and `TestRelayAlphaSearchChannelGate` cover price selection, marker idempotency, completion, route passthrough, and provider gating. |
| P-24 | Accepted-work tool-surcharge and combined-quota overflow | PASS | After upstream acceptance, an inexact or overflowing model conversion, tool conversion, or exact combined sum returns a stable settlement failure while leaving the durable reservation dispatched for recovery; it neither refunds accepted work nor writes a false consume log. `TestOrdinaryRelayRejectsModelQuotaClampAfterAcceptedWork`, `TestOrdinaryRelayRejectsToolQuotaClampAfterAcceptedWork`, and `TestOrdinaryRelayRejectsCombinedModelAndToolQuotaOverflow` exercise all three branches through the production relay settlement path. |

## Funding choice, reservation, settlement and recovery

| ID | Billing boundary | Status | Deterministic evidence or exact gap |
|---|---|---|---|
| L-01 | Wallet/subscription preference normalization and all four modes | PASS | `TestNormalizeBillingPreference` and `TestFundingSessionPreferenceDispatch` execute `subscription_first`, `wallet_first`, `subscription_only`, and `wallet_only`, including the subscription wallet-overflow gate. |
| L-02 | Billing-preference lookup failure | PASS | `TestNewFundingSessionReturnsBillingPreferenceReadFailure` proves a database read failure is returned rather than silently selecting a funding source. |
| L-03 | Subscription pre-consume idempotency, caller binding, and quota-window fencing | PASS | A server request ID owns the ledger row and captures `usage_epoch`; exact replay is a no-op, cross-user/corrupt replay fails, and a delayed old-window operation cannot mutate current-window usage. `TestPreConsumeSubscriptionValidationAndLedger`, `TestPreConsumeSubscriptionRejectsCrossUserReplayAndCorruptCounters`, and `TestUsageEpochFencesDelayedSubscriptionRefundAndSettlement` cover the boundary. |
| L-04 | Subscription choice, earliest expiry, lazy reset and wallet fallback | PASS | `TestPreConsumeSubscriptionCandidateSelection`, `TestPreConsumeSubscriptionLazyReset`, and `TestFundingSessionPreferenceDispatch` cover deterministic selection/reset and allowed/denied fallback. |
| L-05 | Subscription reservation refund idempotency and stale-epoch terminalization | PASS | `TestRefundSubscriptionPreConsumeIdempotent`, `TestRefundSubscriptionPreConsumeRejectsCorruptLedgerWithoutTransition`, and `TestUsageEpochFencesDelayedSubscriptionRefundAndSettlement` prove exact-once refund, fail-closed corrupt-ledger behavior, and terminalization without erasing a newer quota window. |
| L-06 | Concurrent subscription reservation/consumption overspend prevention | PASS | `TestConcurrentSubscriptionPreConsume` and `TestConcurrentSubscriptionConsumeDoesNotOverspend` prove the tested concurrent boundary does not overspend. |
| L-07 | Joint funding plus limited-token reservation | PASS | `service.NewRelayQuotaReservation` reserves wallet or subscription funding, the limited token, and the durable operation row in one transaction. `TestRelayQuotaReservationCreationRollsBackHoldsWhenLedgerWriteFails`, `TestSubscriptionTokenRelayReservationRollsBackEveryCreationBoundary`, and `TestRelaySubscriptionBillingEndToEnd` cover wallet/subscription success and each injected creation boundary. |
| L-08 | Insufficient token leaves user funding untouched | PASS | `TestRelayQuotaReservationInsufficientTokenDoesNotTouchUser` proves the joint reservation rolls back the wallet debit when token capacity is insufficient. |
| L-09 | Actual-versus-reserved settlement and unused-hold refund | PASS | `TestRelayQuotaReservationSettlesUserAndTokenAtomically` proves shortfall reconciliation and terminal idempotency; `TestRelayQuotaReservationRefundsUnusedFunding` proves exact-once unused refund. |
| L-10 | Failed settlement retains all original holds | PASS | `TestRelayQuotaReservationSettlementFailureRetainsBothReservations` proves an accepted-work settlement failure does not release either user or token quota. |
| L-11 | User lifetime usage/request counters and limited-token lifetime usage | PASS | `TestRelayQuotaReservationSettlesUserAndTokenAtomically` proves user `UsedQuota`, `RequestCount`, token `UsedQuota`, balances, and the terminal marker change together exactly once. |
| L-12 | Persistent unlimited-token usage and synthetic Playground tokens | PASS | `TestRelayQuotaReservationPersistsUnlimitedTokenAndChannelUsage` proves a persisted unlimited token's `RemainQuota` is unchanged while `UsedQuota` advances once and the durable row records the invariant. `TestRelayQuotaReservationSupportsPlaygroundSyntheticUnlimitedToken` proves an ID-zero synthetic token settles the wallet, user counters and durable reservation without attempting a nonexistent token-row update. |
| L-13 | Channel lifetime usage in the same settlement transaction | PASS | `TestRelayQuotaReservationPersistsUnlimitedTokenAndChannelUsage` proves the selected channel counter advances once; `TestRelayQuotaChannelFailureRollsBackAndRecoversAllAccounting` proves a channel overflow rolls back user/token/counter changes and later recovery commits all of them together. |
| L-14 | Corrupt and overflowing persisted accounting values | PASS | `TestRelayQuotaReservationRejectsQuotaAndCounterOverflow`, `TestRelayQuotaReservationTokenUsageOverflowRollsBackUser`, `TestRelayQuotaReservationRefundRejectsTokenBalanceOverflow`, `TestCommitReservedUsageRejectsCorruptWalletQuotaAndRetries`, `TestCommitReservedUsageRejectsCorruptUserCounters`, and `TestCommitReservedUsageRejectsCorruptSubscriptionUsageAndRetries` cover input, balance, usage and request-counter failures without partial mutation. |
| L-15 | Concurrent settlement is exactly once | PASS | `TestFundingSessionConcurrentSettleIsExactlyOnce` and `TestRelayQuotaReservationConcurrentDurableClaimsSettleOnce` prove process-local and durable-claim serialization. |
| L-16 | Zero-price subscription compensation | PASS | Subscription reservation uses a one-unit ledger hold when the calculated price is zero, then releases it on acceptance. `TestCommitAcceptedPerCallIsAtomicAndIdempotent` proves the resulting zero charge and one request count. |
| L-17 | Idempotency-ledger cleanup versus live reservations | PASS | `TestCleanupSubscriptionPreConsumeRecordsPreservesNonterminalRelayReservations` proves cleanup does not prune the auxiliary record for a nonterminal durable relay; `TestSubscriptionRelayRefundRecoversWhenAuxiliaryLedgerWasPruned` proves the durable reservation remains sufficient for a safe refund. |
| L-18 | Durable reservation creation and ambiguous create result | PASS | `TestRelayQuotaReservationCreationRollsBackHoldsWhenLedgerWriteFails` and `TestRelayQuotaReservationAmbiguousCreationRecoversByUniqueMarker` cover rollback before commit and verification after an ambiguous driver result. |
| L-19 | Durable pre-dispatch marker and ambiguous marker result | PASS | `TestRelayQuotaReservationAmbiguousDispatchUsesDurableMarker` plus ordinary/realtime protocol tests prove the marker is stored before contact and its unique state resolves an ambiguous commit result. |
| L-20 | Durable pending-settlement recovery | PASS | `TestRelayQuotaSettlementFailureIsRecoveredFromDurableIntent` and `TestRelayQuotaChannelFailureRollsBackAndRecoversAllAccounting` prove that actual quota and selected channel survive the failure and the worker retries the full atomic settlement. |
| L-21 | Durable pending-refund recovery | PASS | `TestRelayQuotaRefundFailureRollsBackAndWorkerRetries` proves a partial refund cannot commit and the worker later releases both funding and token holds exactly once. |
| L-22 | Ambiguous settlement/refund terminal commit | PASS | `TestRelayQuotaReservationAmbiguousCommitUsesTerminalMarker` and `TestRelayQuotaReservationAmbiguousRefundUsesTerminalMarker` prove the terminal durable marker resolves post-commit driver errors without replaying money movement. |
| L-23 | Concurrent recovery claims | PASS | `TestRelayQuotaReservationConcurrentDurableClaimsSettleOnce` proves only one leased claimant applies settlement. |
| L-24 | Expired processing lease takeover | PASS | `TestRelayQuotaReservationExpiredProcessingLeaseIsRecovered` proves abandoned processing work is reclaimable. |
| L-25 | Expired undispatched hold | PASS | `TestExpiredRelayQuotaHoldIsRefundedByRecoverySweep` and `TestExpiredSubscriptionRelayHoldIsDurablyRefunded` prove a never-dispatched wallet or subscription hold is safe to refund automatically. |
| L-26 | Expired dispatched hold | PASS | `TestExpiredDispatchedRelayQuotaHoldRequiresManualReviewWithoutRefund` proves possible accepted work is moved to `manual_review` with its holds retained. |
| L-27 | Bounded recovery attempts and diagnostic size | PASS | `TestRelayQuotaReservationInterruptedFinalAttemptBecomesManualReview` and `TestRelayQuotaReservationManualReviewCapsPersistedError` prove final-attempt escalation and bounded persisted errors. |
| L-28 | Periodic recovery scheduler wiring | PASS | `service.runPeriodicJobs` invokes `ReconcileRelayQuotaReservations` under a distributed task lease; `TestPeriodicJobsSchedulesRelayQuotaReconciliationUnderLease` executes the dispatcher and proves the handoff. |
| L-29 | Operator list/retry/resolve workflow for `manual_review` | PASS | Bounded, filtered, secret-free root views and audited idempotent retry-to-settle/refund actions are implemented. `TestManualReviewRelayQuotaListIsBoundedFilteredAndSecretFree`, `TestRetryManualReviewSettlementIsAtomicAuditedAndIdempotent`, `TestRetryManualReviewRefundReturnsExactHoldAndAuditsOnce`, and `TestRelayQuotaReservationManualReviewRootWorkflow` cover service and HTTP boundaries. |

## Ordinary, streaming, realtime and Jimeng paths

| ID | Billing boundary | Status | Deterministic evidence or exact gap |
|---|---|---|---|
| R-01 | Ordinary relay reserves before upstream and prevents concurrent overspend | PASS | `TestOrdinaryRelayReservationPreventsConcurrentTokenOverspend`, `TestOrdinaryRelayPersistsDispatchedStateBeforeUpstreamCall`, and `TestOrdinaryRelayDispatchMarkerFailureNeverContactsUpstream` prove ordering and rejection before contact. |
| R-02 | Authorized auto-group selection and actual-group settlement | PASS | `TestAutoGroupRelayUsesAuthorizedFallbackAndActualGroupBilling` proves selection is restricted to authorized groups and the final selected group, not the literal `auto`, prices and labels the charge. |
| R-03 | Worst-case pre-reservation across cross-group retries | PASS | `TestAutoGroupRelayUsesAuthorizedFallbackAndActualGroupBilling` configures different authorized group ratios, asserts the durable hold equals the highest retry-group estimate before dispatch, and proves final settlement uses the lower selected group's actual price. |
| R-04 | Non-stream success buffering until settlement commits | PASS | `TestOrdinaryRelaySettlementFailureRollsBackUserAndTokenTogether` proves an upstream success body is discarded and replaced by a settlement error when the accounting transaction fails. |
| R-05 | Definitive no-use failure refund | PASS | `TestOrdinaryRelayRefundFailureIsReturnedAndNeverOverCredits`, `TestRelaySubscriptionRefundOnUpstreamFailure`, and `TestOpenAICompatibleProviderErrorRefundAndMapping` cover safe refund, rollback on refund failure, and absence of consumption. |
| R-06 | Usage-bearing failure and partial-stream terminal accounting | PASS | `TestOrdinaryRelayUsageBearingFailureIsChargedOnceAndNeverRetried` and `TestOrdinaryRelayPartialStreamIsSettledAndNeverRetriedOrRefunded` prove accepted output is charged once and cannot be retried/refunded. |
| R-07 | Stream completion estimation when providers omit usage | PASS | OpenAI, Claude, and Gemini stream adaptors estimate text where possible, while the outer lifecycle retains a nonzero prompt reservation fallback. `TestSuccessfulStreamWithoutProviderUsageSettlesPromptEstimate` proves a successful usage-less stream settles rather than becoming free or refunded. |
| R-08 | OpenAI-compatible ordinary provider settlement | PASS | `TestOpenAICompatibleProvidersWireAndSettlement` runs provider-specific request/response normalization through the durable lifecycle and asserts user/token balances, counters, log, and terminal reservation; `TestRelayChatCompletionsEndToEnd` covers the base OpenAI path. |
| R-09 | Native Claude and Gemini ordinary/stream billing wiring | PASS | Native and converted paths use the shared durable lifecycle. `TestOpenAIClientNativeProviderToolStreamsSettleEndToEnd` asserts streamed tool conversion, prompt/completion usage, consume log, quota, and terminal settlement for both providers; native client-format tests cover passthrough and cache-aware Claude settlement. `TestGeminiNativeTieredMultimodalUsageSettlesExactAccounting` additionally proves exact native Gemini multimodal settlement. |
| R-10 | Realtime reservation before either WebSocket handshake and before upstream dial | PASS | `TestRealtimeRejectsQuotaBeforeEitherHandshake`, `TestRealtimeUpstreamHandshakeFailureRefundsReservation`, and `TestRealtimeDispatchMarkerFailurePreventsUpstreamDialAndRefunds` prove the ordering and safe failure behavior. |
| R-11 | Realtime unique-response accounting | PASS | `TestRealtimeAccountsEachUniqueResponseAndRefundsDisconnectReservation` proves duplicate `response.done` IDs are ignored, each unique response gets one settlement, and the next unused hold is refunded. |
| R-12 | Realtime malformed usage, size limit and disconnect handling | PASS | `TestNormalizeRealtimeUsage`, `TestRealtimeMalformedUsageChargesFallbackAndTerminates`, `TestRealtimeOversizedUpstreamMessageRefundsAndTerminates`, and `TestRealtimeDisconnectBeforeUsageRefundsReservation` cover normalization/fallback and terminal behavior. |
| R-13 | Realtime settlement/log failures after accepted usage | PASS | `TestRealtimeSettlementFailureRetainsReservation` proves no refund after failed settlement; `TestRealtimeLogFailureIsSurfacedAfterChargeWithoutDoubleRefund` proves a committed charge is not undone because its separate log write failed. |
| R-14 | Jimeng frame-duration/per-call reservation | PASS | `TestComputePerCallQuotaAndTokenReservation` proves per-call units; `TestJimengOfficialTaskContract` verifies 121 frames bill one unit and 241 frames bill two, with reservation before provider submission. |
| R-15 | Jimeng accepted-task atomic and idempotent commit | PASS | `TestCommitAcceptedPerCallIsAtomicAndIdempotent` and `TestConcurrentJimengPendingSettlementCommitsOnce` prove task persistence, funding, user/token/channel counters, and pending-marker cleanup commit once. |
| R-16 | Jimeng preflight, provider ambiguity, refund and fetch recovery | PASS | `TestJimengAcceptedTaskCommitFailureRecoversOnFetch` proves accepted settlement recovers exactly once on fetch even after the original limited token is soft-deleted and the selected channel is deleted. `TestJimengFallbackWriteFailureRecoversFromEncryptedDatabaseOperation`, `TestJimengPrimaryRecoveryAllowsDispatchWhenSecondaryJournalIsUnavailable`, `TestJimengReservationStorageFailuresAreInternalAndCompensated`, `TestJimengRefundFailureRemainsDurableForAnotherWorker`, and `TestJimengSecondaryRecoveryCleanupFailureCannotReplaySettlement` cover the remaining tested failure windows. |
| R-17 | Jimeng pre-dispatch reservation crash atomicity | PASS | Funding, limited-token hold, task row, and `jimeng_task_operations` recovery row are created in one database transaction before dispatch. `TestJimengAtomicCreationRollsBackEveryPredispatchBoundary` injects every boundary and proves no orphan hold or task survives. |
| R-18 | Cluster-visible autonomous Jimeng recovery | PASS | The primary recovery journal is database-backed, encrypted, lease-fenced, and scheduled independently of client fetch; a secondary encrypted file is emergency evidence only. `TestJimengDispatchingRecoverySurvivesRestartWithoutRedispatchOrGuessingCharge`, `TestJimengDurableUnknownPendingOutcomeSettlesWithoutClientFetch`, `TestJimengSubmittedRecoverySettlesAndPollsWithoutClientFetch`, and lease/fairness tests cover restart and autonomous recovery. |
| R-19 | Reference billing for non-Jimeng asynchronous task providers | PASS | Ali, Doubao, Gemini Veo, Hailuo, Kling, Midjourney, OpenAI/Sora, Suno, Vertex Veo, and Vidu all reserve immutable reference pricing before provider I/O, settle accepted work atomically, retain ambiguous dispatch without replay, and reverse definitive or terminal failure exactly once through provider-neutral TaskOperation/accounting identities. `TestAliWanTerminalFailureReversesSettledChargeExactlyOnce`, `TestDoubaoAcceptedTaskTerminalFailureRefundsHeldQuotaExactlyOnce`, `TestGeminiVeoTerminalFailureReversesSettledChargeExactlyOnce`, `TestHailuoTerminalFailureReversesSettledChargeExactlyOnce`, `TestKlingTerminalFailureRefundsAndAmbiguousDispatchStaysHeld`, `TestMidjourneyFailureRefundAmbiguityAndUndispatchedSafety`, `TestVideoTaskTerminalFailureReversesSettledQuotaExactlyOnce`, `TestSunoFailurePollReversesExactlyOnceAndCannotRegress`, `TestVertexVeoTerminalProviderFailureReversesExactSettlement`, `TestViduTerminalFailureReversesSettledCharge`, and `TestTaskOperationAccountingSQLiteLifecycle` cover every reference family; the external MySQL/PostgreSQL TaskOperation gate proves the shared server-dialect boundary. |

## Wallet, subscription purchase and payment gateways

| ID | Billing boundary | Status | Deterministic evidence or exact gap |
|---|---|---|---|
| W-01 | Redemption issuance, credit atomicity, and idempotency | PASS | Redemption batch keys use checked cryptographic entropy and abort without inserting codes if it is unavailable. `TestSecurityIssuanceEntropyFailureLeavesNoMutation`, `TestRedeemCreditsOnce`, `TestConcurrentRedeemCreditsExactlyOnce`, `TestRedeemRollsBackClaimWhenCreditFails`, and `TestRedeemRejectsOverflowWithoutConsumingCode` cover issuance failure, duplicate, concurrent, storage-failure, and overflow cases. |
| W-02 | Daily check-in credit atomicity | PASS | `TestCheckInOncePerDay`, `TestCheckInRollsBackRecordWhenCreditFails`, and `TestCheckInRejectsOverflowWithoutRecording` prove the award row and wallet credit commit together. |
| W-03 | Top-up creation, secure order identity, price, discount, group ratio and money normalization | PASS | The trade/order identifier uses checked cryptographic entropy before persistence; `TestSecurityIssuanceEntropyFailureLeavesNoMutation` proves entropy failure leaves no top-up row. `TestTopUpPricingPreservesReferenceFormula`, `TestTopUpPricingRejectsInvalidGroupRatiosAndOverflow`, `TestTopUpMoneyNormalizationAndExactComparison`, `TestCreateTopUpRejectsZeroMoney`, and `TestTopUpRequestsFailClosedOnUnsafeAmountsAndPricing` cover exact-decimal comparison and unsafe inputs. |
| W-04 | Wallet top-up completion atomicity/idempotency | PASS | `TestTopUpCompleteIdempotent`, `TestConcurrentTopUpCompletionCreditsExactlyOnce`, `TestTopUpCompletionRollsBackStatusWhenCreditFails`, and `TestTopUpCompletionRejectsUntrustedAmountAndOverflow` prove pending-to-success plus wallet credit is one transaction. |
| W-05 | Administrator manual top-up recovery | PASS | `TestManualCompleteTopUpRecoveryBoundsStatusAndIdempotency`, `TestConcurrentManualCompleteTopUpCreditsExactlyOnce`, `TestTopUpAdminDatabaseFailuresFailClosed`, and `TestAdminCompleteTopUpContract` prove a root can recover an already-created pending order even when new-payment compliance is disabled, while ownership, identifier/amount/status bounds, idempotency, concurrency, route authorization, and storage failures remain enforced. |
| W-06 | Balance-funded subscription purchase | PASS | `TestPurchaseAndConsumeSubscription`, `TestPurchaseSubscriptionWithBalanceGuards`, and `TestSubscriptionBalancePay` prove guarded wallet deduction, subscription creation, order/top-up records and consumption. |
| W-07 | Subscription plan financial validation and purchase cap | PASS | `TestSubscriptionPlanFinancialValidation`, `TestSubscriptionFulfillmentRejectsOversizedPersistedPlan`, and `TestConcurrentSubscriptionOrderCompletionIsSingleEffect` cover malformed/oversized plans and serialized fulfillment. |
| W-08 | Calendar reset/expiry, immutable cadence, usage epochs, and partial-batch failure | PASS | Reset uses DB time, advances `usage_epoch` with CAS, performs constant-time UTC catch-up from an immutable entitlement snapshot, and expiration drains all due users while reconciling overlapping upgrade groups. `TestResetSubscriptionQuotaCalendarWalk`, `TestUsageEpochFencesExactPartialAndOverageSettlements`, `TestLazyResetUsesImmutableSnapshotAndUTCConstantTimeCatchup`, `TestExpireDueSubscriptionsDrainsUsersAndReconcilesUpgradeChainOnce`, and `TestResetDueSubscriptionQuotasReturnsFailuresAndContinues` cover the boundary. |
| W-09 | Paid subscription order fulfillment and immutable entitlement snapshot | PASS | Completion atomically binds the order to amount/currency/provider session and creates a cadence/group entitlement snapshot that survives plan mutation/deletion; duplicate and racing completion is single-effect. `TestCompleteStripeSubscriptionOrderBindsAmountAndCurrency`, `TestSubscriptionResetCadenceSnapshotSurvivesPlanMutationAndDeletion`, `TestCompleteSubscriptionOrderCASRollsBackOnLifecycleRace`, and `TestConcurrentSubscriptionOrderCompletionIsSingleEffect` prove the boundary. |
| W-10 | Versioned payment-compliance gate | PASS | `TestPaymentComplianceConfirmationContract`, `TestRootOptionSecurityContract`, and `TestOptionAndComplianceRoleGuards` prove current-version confirmation and root-only control. |
| W-11 | EPay wallet checkout/callback local contract | PASS | `TestTopUpEpayPayAndNotifyContract` and `TestTopUpEpayUnconfigured` prove signed URL construction, fail-closed configuration, callback signature/provider/method/exact-money binding, and idempotent local completion with fixtures. |
| W-12 | Stripe webhook body/signature/state machine | PASS | `TestVerifyStripeSignature`, `TestVerifyStripeSignatureTimestampTolerance`, `TestVerifyStripeSignatureRejectsMalformedOrDuplicateTimestamp`, `TestVerifyStripeSignatureAcceptsAnyValidV1`, `TestVerifyStripeSignatureBoundsHeaderWork`, `TestStripeWebhookRejectsOversizedBody`, and `TestStripeWebhookPaymentStateMachine` cover the bounded local verifier and accepted event states. |
| W-13 | Stripe wallet completion with local fixtures | PASS | The local order is persisted before checkout, binds immutable money/currency/customer metadata, retains ambiguous provider outcomes for reconciliation, and credits only an exact paid session. `TestTopUpStripePersistsBeforeCheckoutAndMarksProviderFailure`, `TestTopUpStripeAmbiguousCheckoutOutcomeStaysPending`, `TestCompleteStripeTopUpBindsAmountAndCurrencyInsideSettlement`, `TestBoundStripeTopUpRequiresExactSessionAndQuarantinesLegacyPending`, and webhook tests prove the boundary. This is not a live Stripe result. |
| W-14 | Stripe subscription completion with local fixtures | PASS | Capacity is reserved before checkout, immutable entitlement and economic snapshots are stored, ambiguous outcomes remain recoverable, and exact paid fulfillment is single-effect. `TestSubscriptionStripePersistsBeforeCheckoutAndMarksProviderFailure`, `TestConcurrentStripeSubscriptionReservationsCannotOversubscribeCap`, `TestStripeSubscriptionReservationBlocksAllPurchasePathsAndFulfillsSnapshot`, and webhook tests prove the boundary. This is not a live Stripe result. |
| W-15 | Stripe initiation/reconciliation, economic binding, and reversal policy | PASS | Wallet/subscription orders are durable before the provider call; immutable request snapshots, bounded DB-time leases, stale-worker fencing, paid-session reconciliation, capacity release, corruption quarantine, and exact customer/owner/money/currency binding are implemented. Refund/dispute events move matched or unmatched cases to durable manual review rather than guessing at reversals. `TestStripeCheckoutReconciliationFulfillsAmbiguousWalletCreateExactlyOnce`, `TestStripeCheckoutReconciliationFulfillsSubscriptionFromImmutableEntitlement`, `TestStripeCheckoutReconciliationLeaseAllowsOnlyOneProviderCall`, `TestStripeCheckoutReconciliationStaleLeaseCannotCredit`, `TestStripeCheckoutCorruptSnapshotMovesToManualReview`, `TestStripeRefundAndDisputePolicyOnlyMarksManualReview`, and `TestUnmatchedStripeDisputeCreatesDurableManualCorrelationAudit` cover the local state machine. |
| W-16 | EPay subscription checkout/callback | PASS | The authenticated checkout stores an exact two-decimal USD amount, payment method, immutable entitlement and purchase-cap reservation before returning locally generated signed EPay parameters. Public GET/POST notify and return callbacks validate bounded single-value fields, merchant, constant-time signature, trade status, method and exact money; notify returns bare `success`/`fail`, while return redirects to the reference wallet result states. Fulfillment creates the subscription/top-up/audit and consumes the reservation in one idempotent transaction. `TestSubscriptionEpayCheckoutNotifyAndReturnContract`, `TestSubscriptionEpayCheckoutFailsClosedBeforeOrderCreation`, `TestBoundEpaySubscriptionFulfillmentIsAtomicIdempotentAndImmutable`, and `TestConcurrentBoundEpaySubscriptionCallbacksHaveSingleEffect` cover route contracts, signed forgeries, disabled/configuration gates, rollback, snapshot mutation, replay and concurrency without external network. This is not a live EPay result. |
| W-17 | Creem wallet and subscription payment | PASS | The four validated, atomically hot-reloadable Creem settings drive a bounded direct HTTP client with fixed production/test endpoints and injectable local transport. Authenticated wallet/subscription creation stores the credential-free checkout request, fingerprint, provider idempotency key, exact product/money/currency, direct wallet quota or immutable subscription entitlement, and purchase-cap reservation before any provider call. Definitive 4xx rejection terminalizes the order and releases subscription capacity; transport/5xx/malformed-success ambiguity preserves the durable pending snapshot, whose later correctly signed and economically exact webhook can atomically claim the missing checkout ID. The public callback requires the full availability/compliance gate, a bounded exact raw body, and lowercase-hex HMAC-SHA256; it acknowledges unrelated/unpaid events but fulfills `checkout.completed`/`paid` only when checkout, request, product, minor-unit amount, currency and metadata all match, with settlement/top-up/audit writes committed exactly once. `TestCreemConfigurationValidationAndAtomicHotReload`, `TestCreemConfigurationDefaultsRemainDisabled`, `TestCreemWalletOrderSnapshotsDirectQuotaAndSettlesExactlyOnce`, `TestCreemWalletOverflowRetainsPendingOrderForSafeRetry`, `TestCreemSignedWebhookClaimsCheckoutAfterAmbiguousCreate`, `TestCreemSubscriptionSnapshotsEntitlementAndSettlesIdempotently`, `TestConcurrentCreemWalletAndSubscriptionCallbacksHaveSingleEffect`, `TestCreemWalletAndSubscriptionHTTPContractEndToEnd`, `TestCreemWebhookSignatureAvailabilityAndBodyLimits`, `TestCreemCheckoutFailureClassificationPreservesAmbiguousOrdersAndReleasesDefiniteRejections`, and `TestCreemTopUpInfoAndInvalidRequestContracts` cover the local boundary. This is not a live Creem result. |
| W-18 | Classic Waffo wallet payment | PASS | The pinned reference implements classic Waffo only for wallet amount, checkout, and the shared signed webhook; its only Waffo subscription checkout is the separate Pancake integration retained in W-19. The target's atomically published, bounded configuration selects the exact production/sandbox credentials and fixed endpoint, validates RSA material and HTTPS/loopback callback URLs, and owns the payment-method catalog. The injectable client reproduces the SDK's RSA-SHA256 headers and exact request/response wire contract with bounded direct transport; `E0001`, transport/5xx, malformed or unverifiable success remains durable and pending, while definitive rejection terminalizes safely. Before I/O, checkout persists exact amount/currency, wallet quota, environment, merchant/user/product/method, request fingerprint and provider idempotency identity. Signed webhook settlement must match every snapshot/provider field, can safely bind an ambiguous create, and commits status, wallet credit and audit outbox atomically and exactly once; forged/tampered/legacy orders fail closed and storage errors return a signed retryable failure. As the explicit safety deviation recorded in Known Deviations item 19, only signed `ORDER_CLOSE` closes an order; authorization/in-progress states remain pending instead of being destroyed by the reference's broader non-success branch. `TestBuildWaffoConfigSelectsExactEnvironmentAndDefensiveMethods`, `TestBuildWaffoConfigFailsClosedOnUnsafeValues`, `TestWaffoOptionValidationRejectsBeforePublication`, `TestValidWaffoCallbackURL`, `TestWaffoMoneyFormattingAndBounds`, `TestWaffoWalletOrderSnapshotBindingRetryAndIdempotentCredit`, `TestWaffoSignedSettlementClaimsAmbiguousCreateAndClosesTerminalOrder`, `TestWaffoSettlementFailsClosedOnLegacyOrTamperedSnapshot`, `TestConcurrentWaffoCallbacksCreditOnce`, `TestCreateBoundWaffoTopUpRejectsInvalidSnapshotInputs`, `TestWaffoHTTPWireSnapshotAndRetryableSettlementEndToEnd`, `TestWaffoCheckoutFailuresRemainDurableOrFailTerminally`, `TestWaffoSignatureBoundaryIsInjectable`, `TestWaffoWebhookSignatureBoundsAndTerminalStatuses`, and `TestWaffoClientBoundaryAndTopUpInfoAreInjectedAndAdvertised` cover the local boundary without credentials or external calls. This is not a live Waffo result. |
| W-19 | Waffo Pancake wallet/subscription/catalog/pairing | PASS | The target implements the standalone Pancake wire protocol without the vendor SDK: fixed-host direct TLS transport, no proxy or redirect delegation, bounded requests/responses/headers, deadlines, exact RSA-SHA256 canonical signing and idempotency headers, authenticated checkout token/session validation, and production-then-test webhook-key verification with replay tolerance. Root-only configuration validates RSA keys, IDs, callback URL, unit price and minimum; the seven values publish atomically, a blank key preserves the existing secret only for the same merchant, and option reads never expose the private key. Catalog, pair creation, wallet-product binding, per-plan product creation and product options are operable through bounded endpoints and the localized admin UI. Wallet and subscription orders persist immutable economics, buyer/store/product/mode/request fingerprints and entitlement/capacity snapshots before provider I/O; definitive rejection terminalizes safely, ambiguous creation remains durable, and only an exact signed store/mode/order/user/product/amount/currency settlement can atomically grant value and audit it once. `TestWaffoPancakeProtocolAuthenticatedCheckoutWireAndSignature`, `TestWaffoPancakeProtocolAdminWireAndCatalogFiltering`, `TestWaffoPancakeProtocolBoundsAndGenericErrors`, `TestWaffoPancakeWebhookSignatureReplayAndModeValidation`, `TestSaveWaffoPancakeConfigIsAtomicAndBlankKeyCannotChangeMerchant`, `TestWaffoPancakeWalletOrderBindsEconomicsAndCreditsOnce`, `TestWaffoPancakeSubscriptionReservesCapacityAndUsesImmutableEntitlement`, `TestWaffoPancakeWalletAndSubscriptionHTTPContractEndToEnd`, `TestWaffoPancakeWebhookAvailabilitySignatureAndBodyLimits`, `TestWaffoPancakeCheckoutFailureClassification`, `TestWaffoPancakeRootAdministrationContract`, `waffo-pancake-admin-api.test.ts`, `WaffoPancakeAdminPanel.test.tsx`, `wallet-api.test.ts`, and `WalletView.test.tsx` cover the local boundary. This is not a live Pancake transaction. |
| W-20 | Live EPay wallet transaction/webhook | BLOCKED_EXTERNAL | Local signed fixtures pass W-11. A real exchange needs a configured EPay merchant, reachable callback and external network; none is available in this audit environment. |
| W-21 | Live Stripe wallet checkout/webhook | BLOCKED_EXTERNAL | Local fixtures pass W-12/W-13. A real exchange needs a Stripe account, secret, price, webhook secret, reachable callback and network; none is available. |
| W-22 | Live Stripe subscription checkout/webhook | BLOCKED_EXTERNAL | Local fixtures pass W-12/W-14. A real subscription-mode checkout also needs a live/sandbox product-price configuration and callback delivery; none is available. |

## Logs, reminders and external database proof

| ID | Billing boundary | Status | Deterministic evidence or exact gap |
|---|---|---|---|
| A-01 | Consumption-log schema and checked storage result | PASS | `TestRecordConsumeLogCheckedFailsClosedWithoutAuthoritativeClock`, `TestRelaySubscriptionBillingEndToEnd`, and `TestOpenAICompatibleProvidersWireAndSettlement` prove checked persistence, fail-closed authoritative-clock handling, and core quota/funding/reservation fields. |
| A-02 | Log-sink failure after an already-committed charge | PASS | `TestOrdinaryRelayLogSinkFailureFallsBackWithoutTurningSuccessIntoRetry` proves an ordinary accepted response and charge remain successful while the failed sink is durably replaced by a secret-free primary-database outbox event. `TestRealtimeLogFailureIsSurfacedAfterChargeWithoutDoubleRefund` and `TestJimengConsumeLogFailureKeepsAcceptedResponseAndCharge` prove their established accepted-work paths likewise avoid refund or client replay after the charge commits. |
| A-03 | Hourly quota-data aggregation and retry | PASS | `TestQuotaDataHistogramWritePath`, `TestSaveQuotaDataCacheRetainsQueryFailureForRetry`, `TestSaveQuotaDataCacheRetainsUpdateFailureAndRetriesOnce`, and `TestLogQuotaDataCheckedRejectsAggregateOverflow` cover bucket writes, retention on failure and overflow rejection. |
| A-04 | Saturation marker in the durable consumption log | PASS | `TestQuotaSaturationMarkerPersistsInConsumptionAuditLog` drives an oversized usage through settlement and reads back the durable `quota_saturation` marker; arithmetic clamp tests cover the underlying bounds. |
| A-05 | Durable financial/consumption audit outbox | PASS | Checked audit writes require the primary DB clock before a sink write and use it for emitted audit `created_at` plus every outbox transition. A failed separate log sink falls back to a primary-DB outbox with bounded payload/error text, DB-time leases, fencing, backoff, and ambiguous-commit reconciliation. Pending payloads remain available until delivery; successful delivery atomically replaces them with SHA-256 receipts. Before serving, the indexed batched startup scrub converts all legacy delivered payloads; each leased periodic delivery pass independently scrubs at most 500 more legacy delivered rows, even if another delivery fails. Receipts preserve identical-event replay idempotency while rejecting ID/content collisions, and sink event-ID lookup makes delivery exactly once. Older workers are not receipt-aware and can leave raw delivered payloads or mishandle receipt-only replay rows, so a mixed-version rollout must coordinate or drain them as described in Known Deviations item 25. `TestCheckedAuditWriteFallsBackAndWorkerDeliversExactlyOnce`, `TestAuditOutboxReconcilesAlreadyDeliveredEventID`, `TestAuditOutboxEnqueueIsIdempotentButRejectsEventCollision`, `TestCheckedAuditWriteRequiresPrimaryClockEvenWithHealthySink`, `TestAuditOutboxDeliveryUsesDurableClockWithoutRewritingReplayPayload`, `TestAuditOutboxRefreshesAuthoritativeClockForEachLeaseTransition`, `TestScrubDeliveredAuditLogPayloadsMigratesLegacyRowsAndPreservesPending`, `TestPeriodicAuditDeliveryScrubsLegacyPayloadEvenWhenDeliveryFails`, `TestAuditOutboxLeaseContentionAndStaleWorkerFencing`, and `TestCheckedAuditWriteFailsClosedBeforeSinkWhenPrimaryClockIsUnavailable` prove the local state machine. |
| A-06 | Low-quota reminder threshold behavior | PASS | `TestQuotaReminderSendsBelowThreshold`, `TestQuotaReminderSkipsWhenNotNeeded`, and `TestQuotaReminderDisabledByOption` cover send, suppression and option disablement. |
| A-07 | Recorded MySQL/PostgreSQL accounting transactions | PASS | The recorded 2026-09-05 assembled-tree suite passed separately on isolated MySQL and PostgreSQL 17: `TestRelayAccountingExternalDatabaseConcurrency`, `TestAuditOutboxExternalDatabaseLeaseAndDelivery`, `TestSubscriptionUsageEpochExternalDatabaseLifecycle`, and `TestJimengExternalDatabaseAtomicLifecycle` exercise server-dialect locking, rollback, leases/fencing, delivery, epoch isolation, and atomic task/accounting recovery. This proves the named database boundaries, not a current/final-tree result or a live upstream model or payment-gateway exchange. |

## Recorded verification (2026-09-05 assembled tree)

The following commands and results are recorded evidence from the 2026-09-05
assembled working tree; they are not a current or final-snapshot verification:

```text
go test ./common ./pkg/billingexpr ./setting ./service ./relay ./controller -count=1
PASS: all six packages

go test ./controller -run '^TestJimengOfficialTaskContract$' -count=1
PASS: focused Jimeng controller contract

go test ./controller -run 'Test(Admin.*TopUp|AdminSubscription|OptionAndCompliance|PaymentCompliance|RootOption|Stripe|Subscription|TopUp)' -count=1
PASS: controller payment, top-up, subscription and compliance selection

go test ./service -run '^TestRelayQuotaReservationSupportsPlaygroundSyntheticUnlimitedToken$' -count=1
PASS: synthetic Playground unlimited-token reservation and settlement

TOKENROUTER_TEST_ALLOW_SCHEMA_RESET=1 TOKENROUTER_TEST_SQL_DSN=<isolated-mysql-or-postgresql-dsn> go test -p 1 ./model ./service ./relay -run '^(TestDatabaseMatrixExternalMigration|TestRelayAccountingExternalDatabaseConcurrency|TestAuditOutboxExternalDatabaseLeaseAndDelivery|TestSubscriptionUsageEpochExternalDatabaseLifecycle|TestTaskOperationAccountingExternalDatabaseLifecycle|TestJimengExternalDatabaseAtomicLifecycle)$' -count=1
PASS: complete suite passed separately on MySQL and PostgreSQL 17

go test ./... -count=1
PASS: full Go suite on the recorded 2026-09-05 assembled tree
```

The first sandboxed attempt at the full and focused HTTP suites could not bind the local ephemeral ports used by `httptest` (`bind: operation not permitted`). Repeating the same commands with local-loopback access produced the passing results above; this was an environment restriction, not an assertion failure.

Documentation and evidence checks:

```text
awk -F'|' '/^\| [PLRWA]-[0-9][0-9] / {gsub(/^ +| +$/, "", $4); count[$4]++; total++} END {print "PASS", count["PASS"]+0; print "IN_PROGRESS", count["IN_PROGRESS"]+0; print "NOT_STARTED", count["NOT_STARTED"]+0; print "BLOCKED_EXTERNAL", count["BLOCKED_EXTERNAL"]+0; print "TOTAL", total+0}' docs/parity/BILLING_MATRIX.md
PASS 98
IN_PROGRESS 0
NOT_STARTED 0
BLOCKED_EXTERNAL 3
TOTAL 101

awk '/^\| [PLRWA]-[0-9][0-9] /' docs/parity/BILLING_MATRIX.md | rg -o 'Test[A-Za-z0-9_]+' | sort -u | wc -l
318

comm -23 <(awk '/^\| [PLRWA]-[0-9][0-9] /' docs/parity/BILLING_MATRIX.md | rg -o 'Test[A-Za-z0-9_]+' | sort -u) <(rg -o '^func Test[A-Za-z0-9_]+' --glob '*_test.go' . | sed 's/.*func //' | sort -u)
no output (all 318 cited test names exist)

git diff --check -- docs/parity/BILLING_MATRIX.md
PASS
```
