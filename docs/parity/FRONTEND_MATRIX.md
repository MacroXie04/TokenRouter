# Frontend Matrix

Strict frontend comparison against reference commit `ccd535ef8e50cf6e5846a59278c40b7ff59d1b7d` (audited 2026-09-06). The target is an independently designed single-page application with pathname-aware routing, history navigation, setup/auth/admin/root guards, safe post-login redirects, a shared responsive authenticated shell, and dedicated feature domains for the operative console workflows. Route policy is framework-independent code in `web/src/lib/router.ts` and has deterministic tests. `PASS` means the reachable target behavior is complete, localized, bounded, and supported by rendered or contract evidence; it does not require dormant reference strings or a mutable UI for an intentionally immutable safety, accounting, deployment, or trust boundary.

Verification for this snapshot:

- `npm test -- --run`: **98 files / 751 tests passed**. Coverage includes the shared authenticated shell and complete refresh-cookie recovery lifecycle; role/personal sidebar policy; persistent locale preference; exact route guards and redirects; dedicated home, pricing/performance, dashboard, model/deployment, playground, profile, subscription, usage-log, channel, user, relay-key, redemption, wallet, ranking, system-info, and system-settings workflows; complete registration/password/OAuth/WeChat/Telegram/passkey/2FA surfaces; bounded API adapters; secret non-redisplay; cancellation and stale-response handling; redacted failures; and accessible loading, empty, success, confirmation, and retry states.
- `npm run typecheck`, `npm run lint`, and `npm run build`: passed.
- `node scripts/check-translations.mjs --self-test` and the live checker passed: all seven locales have identical **2,644-key** sets and cover all **2,536 runtime keys** found across 110 source files (2,019 direct literals plus 517 statically resolved keys). The current report records no duplicate, missing, extra, invalid, untranslated, or interpolation-mismatched entries. A mechanical comparison with the pinned reference's 5,266-key English catalog finds 435 shared keys, 4,831 reference-only keys, and 2,209 target-only keys, as expected for independently designed interfaces.
- The system-settings catalog mechanically contains **7 categories / 40 sections**. Thirty-eight expose the target's supported bounded editor or purpose-built operational workflow. The remaining two are explicit non-editable boundaries: SSRF stays enforced by `SafeDialContext` (with only the documented local-development environment escape), and TokenRouter has no delegated worker URL/shared-key trust plane. Provider secrets and web/dashboard throttles that remain deployment-only follow the target's 12-factor contract rather than representing missing reachable UI.
- No row is `IN_PROGRESS`, `BLOCKED_EXTERNAL`, or an approved `INTENTIONAL_DEVIATION`: reference implementation structure and unsafe or unused knobs are not themselves observable target requirements.

Strict totals: **103 PASS / 0 IN_PROGRESS / 0 NOT_STARTED / 0 BLOCKED_EXTERNAL / 0 INTENTIONAL_DEVIATION / 0 REFERENCE_PLACEHOLDER (103 rows).**

| # | Route / Page / Locale | Detail | Status | Target Evidence / Behavioral Verification |
|---|---|---|---|---|
| 1 | / | Route: home landing page (feature domain: home) \| auth: public | PASS | `App.tsx` explicitly matches `/`, applies the status-driven `SystemBrandProvider`, and supplies the validated announcement timeline to the feature-scoped `HomeView`. Rendered tests verify the rich default Hero/Stats/Features/How-It-Works/CTA composition, brand/logo fallback, the full `HeaderNavModules` policy, anonymous sign-up and in-page documentation actions, catalog-derived inert API examples and pricing links, independently resilient pricing/performance/rankings/About/legal resources, and localized loading/empty/redacted-error states. The shared public notice presents safe Markdown plus the newest-first status timeline, persistent unread aggregation, explicit tabs, and loading/empty/error states; multiline Markdown is preserved without weakening control/bidi/surrogate validation. Bounded operator `/home_page_content` still takes precedence with a validated refresh cache, sanitized Markdown links, scriptless HTML isolation, credential-free external frames, and theme/language messages constrained to the exact frame origin. |
| 2 | /about | Route: about page (feature domain: about) \| auth: public | PASS | `features/public-documents/PublicDocumentView.tsx` fetches the reference-compatible raw string from `/about` and safely handles loading/error/empty states, Markdown, isolated HTML, and sandboxed HTTP(S) iframe URLs. `PublicDocumentView.test.tsx` renders the endpoint, formats, sandbox policy, redacted failures, URL validation, and late-unmount behavior. |
| 3 | /privacy-policy | Route: privacy policy (feature domain: legal) \| auth: public | PASS | The feature fetches `/privacy-policy` and renders bounded Markdown, isolated HTML, an explicit safe external link, or localized loading/error/empty states. Rendered tests cover the endpoint, link sanitation, external-link attributes, size cap, and shared failure states. |
| 4 | /user-agreement | Route: user agreement (feature domain: legal) \| auth: public | PASS | The feature fetches `/user-agreement` with the same bounded format and state contract. Rendered tests cover the endpoint, Markdown and isolated configured HTML; router tests cover the public pathname. |
| 5 | /setup | Route: setup wizard (feature domain: setup); redirects away once setup complete \| auth: public | PASS | `features/setup/SetupView.tsx` implements the four-step database, administrator, mutually exclusive usage-mode, and review flow. It preserves an existing root, requires bounded new credentials and confirmation otherwise, submits the exact conditional mode payload, hides passwords from review, prevents duplicate submits, redacts failures, and is keyboard-native. `SetupView.test.tsx` renders both root states and complete navigation/error/payload behavior; `setup.test.ts` rejects malformed or contradictory status data; `router.test.ts` proves forced-entry/completed-install redirects; `App.redemptions.test.tsx` proves successful initialization clears the in-memory guard before navigating to sign-in. Backend setup tests independently cover the atomic root/marker/mode transaction. All copy exists in seven locales. |
| 6 | /sign-in | Route: sign-in (feature domain: auth) \| auth: public | PASS | `/sign-in` implements password, built-in/custom OAuth, WeChat, Telegram, passkey, Turnstile, legal-consent, 2FA handoff, safe-return, and authenticated-redirect behavior from bounded public status. Forty-four `LoginView` tests cover exact transports, feature-policy combinations, callback/error handling, unsupported browsers, duplicate submission, cancellation/unmount safety, malformed responses, and secret-redacted failures. |
| 7 | /sign-up | Route: sign-up (feature domain: auth) \| auth: public | PASS | `/sign-up` honors global, personal-use, password-registration, verified-email, invitation, legal-consent, and Turnstile policy. It performs the exact verification and registration contracts, enforces matching bounded passwords and editable affiliate codes, separates single-use bot proofs, applies resend cooldowns, and provides localized safe states. Rendered tests cover every policy branch and exact payload. |
| 8 | /register | Route: register (feature domain: auth) \| auth: public | PASS | Matching the reference compatibility route, `/register` redirects to `/sign-up`, preserves the complete query string (including invite/return context), and replaces browser history. `router.test.ts` verifies the exact redirect contract. |
| 9 | /forgot-password | Route: forgot password (feature domain: auth) \| auth: public | PASS | `features/auth/PasswordResetView` uses the exact `GET /api/reset_password?email=&turnstile=` contract with bounded email/challenge input and enumeration-safe success/error text. The status-provided widget fails closed when unavailable, consumes each token once, can be retried explicitly, and resets before another request. A 30-second post-success countdown prevents duplicate mail. Rendered tests prove ungated and configured-gate submissions, exact parameters, provider state, cooldown, and secret-redacted failures. |
| 10 | /reset | Route: reset password (feature domain: auth) \| auth: public | PASS | The route accepts bounded reference `email`/`token` links, invokes the exact backend shape, displays and opportunistically copies the generated password, provides an explicit copy action, and rate-limits retries in the UI. It retains a choose-password extension for emailed codes. Rendered tests cover both modes, clipboard, bounds, cooldown, and error redaction. |
| 11 | /user/reset | Route: user reset link (feature domain: auth) \| auth: public | PASS | The reference alias remounts the same feature for each path/query and implements the complete generated-password/copy/cooldown workflow. The frontend component test exercises the `/user/reset?email=&token=` shape, while router tests cover alias matching and the backend route test proves single-use consumption. |
| 12 | /otp | Route: one-time password (feature domain: auth) \| auth: public | PASS | `features/auth/OtpView` fails closed for absent/oversized flow tokens, validates exact six-digit TOTP, formats and normalizes one-time `XXXX-XXXX` backup codes, disables incomplete submissions, redacts failures, and completes login through `/user/login/2fa`. Five rendered tests cover expiry, both factor modes, payloads, success, API failure, malformed responses, duplicate-submit suppression, and late-response cancellation; backend tests cover atomic flow consumption and backup-code single use. |
| 13 | /oauth | Route: OAuth entry (feature domain: auth) \| auth: public | PASS | The route handles the bounded WeChat callback locally and exposes only status-advertised built-in/custom OAuth and Telegram entry points. Safe return, affiliate forwarding, legal-consent gating, malformed/ambiguous callback rejection, one-time callback handling, provider denial cleanup, cancellation, and exact same-origin transports are rendered and tested. |
| 14 | /oauth/$provider | Route: OAuth provider callback (feature domain: auth) \| auth: public | PASS | `OAuthCallbackForwarder` validates the 64-byte provider slug and an 8 KiB, control-free query string, constructs the exact encoded backend callback, renders 404 for invalid input, and performs replacement navigation. `App.oauth.test.tsx` renders both paths and verifies the exact browser-replacement target and fail-closed bounds; router tests cover parameter decoding. |
| 15 | /401 | Route: 401 error page (feature domain: errors) \| auth: public | PASS | `features/errors/ErrorView.tsx` provides localized 401 copy plus keyboard-operable back, home, and sign-in actions. `ErrorView.test.tsx` renders the page and verifies its labelled main region, copy, and links; route matching is covered separately. |
| 16 | /403 | Route: 403 forbidden page (feature domain: errors) \| auth: public | PASS | `ErrorView` provides localized 403 copy and back/home actions. Rendered tests verify the surface, while `router.test.ts` proves both admin and root guard redirects to it. |
| 17 | /404 | Route: 404 not found page (feature domain: errors) \| auth: public | PASS | Explicit and unmatched paths resolve to the localized 404 surface. Rendered tests verify its accessible heading/actions and router tests verify unknown-path selection. |
| 18 | /500 | Route: 500 error page (feature domain: errors) \| auth: public | PASS | The localized 500 surface provides back/home and safe TokenRouter issue-report actions; custom detail is capped at 512 characters. `AppErrorBoundary.test.tsx` forces a render exception and verifies the boundary redacts the private exception detail. |
| 19 | /503 | Route: 503 error page (feature domain: errors) \| auth: public | PASS | The explicit route and fatal bootstrap path render the localized 503 surface. `ErrorView.test.tsx` covers the rendered page and `AppErrorBoundary.test.tsx` rejects the setup probe to verify bootstrap failure without leaking the exception. |
| 20 | /pricing | Route: model pricing list (feature domain: pricing) \| auth: conditional (public by default; requireAuth enforced when module access configured) | PASS | `PricingView` consumes the strictly bounded `/api/pricing` catalog and provides canonical URL-backed search, price sorting, group/owner/quota/modality/endpoint/tag filters, group-adjusted prices, accessible card/table modes, 24-row pagination, model links, compact 24-hour performance summaries, and loading/empty/redacted-error/retry/cancellation states. `catalog.test.ts`, `pricing-api.test.ts`, and `PricingView.test.tsx` verify the parser, exact transport, query normalization, combined filters, both presentations, pagination, price semantics, and lifecycle behavior; navigation/router tests retain the conditional public/auth policy. |
| 21 | /pricing/$modelId | Route: model pricing detail (feature domain: pricing) \| auth: conditional (same module-access policy as /pricing) | PASS | The validated decoded model route composes every target-advertised detail dimension: metadata, exact token/request prices, per-group adjustments, supported API method/path rows, a non-evaluating tiered-expression explanation, and group-selectable 24-hour TTFT/latency/success/throughput summaries and series. Six rendered detail tests cover invalid/missing and reactive models, group scoping, dynamic billing, performance success/empty/redacted-error/retry, and stale-request cancellation. |
| 22 | /rankings | Route: usage rankings leaderboard (feature domain: rankings) \| auth: conditional (public by default; requireAuth enforced when configured) | PASS | `features/rankings/RankingsView.tsx` implements the complete observable period workflow: validated `today`/`week`/`month`/`year` URL state, accessible responsive model and vendor histories, token/share/growth leaderboards, model/vendor pricing links, movers/droppers, and explicit loading/redacted-error/retry/empty states with request cancellation. `rankings-api.ts` bounds the envelope, bytes, arrays, buckets, names, timestamps, safe integers, shares, rank order, movement direction, uniqueness and cross-series membership before rendering. Twelve focused API/component/authorization tests cover exact transport, corruption, every section/state, period navigation, links, retry/unmount safety and disabled/auth-required module policy. The additive backend period snapshot is time/cardinality/series bounded and deterministically tested while the established no-query flat response remains compatible. All copy is translated in seven locales. |
| 23 | /_authenticated (layout) | Route layout: authenticated shell; redirects to /sign-in when no user/token \| auth: authenticated | PASS | Authenticated routes preserve pathname/query in a same-origin-only sign-in target and enforce admin/root roles before rendering. On an authenticated 401, the shared API client single-flights one credentialed `/user/auth/refresh`, replays each waiting request at most once, uses a generation marker to avoid a second rotation for late stale 401s, and expires identity only after a definitive refresh rejection or retried 401. Caller aborts remain local, anonymous requests opt out, and transient network/409/429/5xx refresh failures preserve identity for recovery. `router.test.ts`, seven `api.session.test.ts` cases, two rendered `App.session-refresh.test.tsx` bootstrap cases, and the existing expiry integration test verify the lifecycle and safe return navigation. |
| 24 | /channels | Route: channel management (feature domain: channels) \| auth: admin (role >= ADMIN) | PASS | `/channels` is protected by the role guard and granular read/operate/write/sensitive-write permissions. The dedicated feature implements bounded search/filter/sort/pagination, fresh masked detail, complete typed normal and sensitive editing, single/batch/multi-key creation, tests with an explicit model, model discovery, bulk/tag/copy/balance actions, global and per-channel staged upstream updates, consistency repair, and provider-specific multi-key/Codex/Ollama tools. Root-only create/delete and 2FA/passkey-gated key disclosure are independently enforced; revealed keys stay isolated and auto-clear. Fifty-one channel API/component tests cover exact routes, payload allowlists, confirmation, role separation, malformed/secret-bearing responses, cancellation, and every data state. |
| 25 | /chat/$chatId | Route: configured chat launcher (feature domain: chat); malformed IDs redirect to /dashboard \| auth: authenticated | PASS | `features/chat/ChatView.tsx` resolves the bounded numeric preset against the public `chats` catalog, handles missing/custom-app/web presets, obtains only the caller's first enabled key through the ownership-enforced disclosure endpoint when required, resolves address/key/base64 client placeholders, and requires an explicit disclosure action before placing a key-bearing URL in a sandboxed, no-referrer iframe. Unsafe schemes, credentials, malformed/duplicate/oversized catalogs, raw upstream failures, late async writes, and unresolved launch URLs fail closed. Pure API and rendered component tests cover every state plus malformed-route redirection, and the App integration test proves the feature receives the decoded route ID. |
| 26 | /chat2link | Route: first configured Web-chat launcher (feature domain: chat) \| auth: authenticated | PASS | The compatibility route selects the first validated Web preset, resolves its optional caller-owned active key and configured server address, then navigates externally only after the user explicitly confirms the key-bearing destination. Empty catalogs, missing keys, unsafe URLs, retry, and key-management escape paths are rendered and tested; explicit confirmation is a safety hardening over an automatic secret-bearing redirect while preserving the launch workflow. |
| 27 | /dashboard | Route: dashboard (redirects to default section) (feature domain: dashboard) \| auth: authenticated | PASS | After the authenticated guard, the landing replacement-redirects to the exact reference default `/dashboard/overview`. `router.test.ts` pins the redirect, anonymous return target, and valid/invalid four-section registry behavior. |
| 28 | /dashboard/$section | Route: dashboard section (feature domain: dashboard) \| auth: authenticated | PASS | The exact `overview`, `models`, `flow`, and `users` registry drives `DashboardView`. It implements account-scoped summaries, model distributions/trends, role-shaped flow paths, administrator-only user analytics, performance metrics, local overview content, bounded URL/date/group/model/node filters, quick ranges, persistent chart preferences, accessible tables/charts, retries, and request cancellation. Thirty-seven focused API/component tests plus App routing tests cover role isolation, exact endpoints, malformed data, hidden dimensions, arithmetic, partial failures, stale requests, and all sections. |
| 29 | /errors/$error | Route: authenticated error detail page (feature domain: errors) \| auth: authenticated | PASS | The authenticated route maps `unauthorized`, `forbidden`, `not-found`, `internal-server-error`, and `maintenance-error` to the five corresponding surfaces and fails unknown names to 404 without echoing input. `ErrorView.test.tsx` verifies the complete map and rendered surfaces; router tests verify the auth guard. |
| 30 | /keys | Route: user API keys (feature domain: keys) \| auth: authenticated | PASS | The tested authenticated guard now opens a focused `RelayTokenManager` without loading unrelated console resources. The route provides masked paginated listing, bounded name search, create/edit policy controls, enable/disable, single and selected-row batch deletion with confirmation, selectable/automatic groups, group-aware model limits, quota/expiry/IP restrictions, and accessible loading/error/empty states. Full credential endpoints are deliberately never called; malformed unmasked list data fails closed. Thirteen focused parser, exact-transport, rendered-workflow, destructive-confirmation, redaction, lifecycle, and console-integration tests substantiate the route. |
| 31 | /models | Route: model list (feature domain: models) \| auth: admin (role >= ADMIN) | PASS | `/models` is admin-guarded and redirects to the dedicated metadata section. `ModelsView` implements bounded model/vendor search, pagination, detail, CRUD/status/delete, missing-model discovery, reviewed upstream synchronization, validation, confirmation, cancellation, and redacted loading/error/empty states. Focused API/component and App tests cover the route and complete metadata workflow. |
| 32 | /models/$section | Route: model section (feature domain: models) \| auth: admin (role >= ADMIN) | PASS | The exact `metadata`/`deployments` registry drives the dedicated feature. Deployments implement settings/connection preflight, search/list/detail, hardware/location/replica/name/price resources, create/update/rename/extend/delete, container status/logs, safe public URLs, write-only credential handling, retry/empty/error states, confirmation, serialization, and cancellation. Twenty-six model/deployment tests plus App routing coverage exercise both sections and fail closed for non-admins. |
| 33 | /playground | Route: API playground (feature domain: playground); redirects to /dashboard when its sidebar module is disabled \| auth: authenticated | PASS | The dedicated `PlaygroundView` loads caller-scoped groups/models and sends a session-only, bounded conversation through the real `/pg/chat/completions` relay. It supports system and multi-turn context, streaming content/reasoning, stop/abort, validated temperature/top-p/max-token parameters, feature policy, loading/error/retry/empty states, and unmount safety without handling an API key. Eighteen API/component tests and an App integration test cover exact payloads, split UTF-8 SSE, malformed/truncated streams, duplicate work, cancellation, and redaction. |
| 34 | /profile | Route: user profile (feature domain: profile) \| auth: authenticated | PASS | `ProfileView` is a dedicated, bounded account/security workflow covering display/password, saved language and personal sidebar policy, notification/privacy preferences, verified email, one-time access-token rotation, 2FA/recovery codes, passkeys with scoped proof, sessions, built-in/custom OAuth, WeChat/Telegram bindings, and confirmed account deletion. Session rows visibly identify the current session, accept the reference Unix-seconds `created_at` contract, and normalize bounded legacy ISO timestamps to Unix seconds during rolling upgrades without exposing session identifiers. Forty-one focused API/WebAuthn/component tests plus App integration cover exact transports, role gates, write-only secrets, serialization, independent retry states, cancellation, redaction, and authoritative account updates. |
| 35 | /redemption-codes | Route: redemption codes (feature domain: redemption-codes) \| auth: admin (role >= ADMIN) | PASS | The role >=10 router guard is asserted specifically for this pathname, and `App.redemptions.test.tsx` proves that an authenticated administrator boots the route into the dedicated redemption tab. That tab renders the complete feature-scoped lifecycle described in row 83 rather than the former compact create-only form. |
| 36 | /subscriptions | Route: subscriptions (feature domain: subscriptions) \| auth: admin (role >= ADMIN) | PASS | The admin-guarded `SubscriptionAdminView` implements bounded plan search/filter/pagination, create/edit/status, payment-compliance preflight, quota resets, user lookup, grant/invalidate/hard-delete lifecycle operations, and root-only Waffo Pancake product creation/binding. Twenty-one API/component tests plus App integration cover exact allowlisted payloads, decimal prices, ownership, role gates, confirmation, mutation serialization, stale-response cancellation, and all loading/empty/error/retry states. Self purchase and preference remain available through the wallet workflow. |
| 37 | /system-info | Route: system info (feature domain: system-info) \| auth: admin (SUPER_ADMIN only) | PASS | `App.tsx` routes this root-guarded path to the dedicated `features/system-info/SystemInfoView` instead of the generic admin overview. The independently designed responsive view presents bounded live/stale node, role/runtime/host/resource and start/heartbeat data plus active/history task status, progress, executor and error detail; nodes poll every 30 seconds and active tasks every 8 seconds, with manual refresh and independent accessible loading/error/empty states. Single and bulk stale cleanup require confirmation, serialize mutations, accept only exact delete counts, use encoded node paths and refresh after success. `SystemInfoView.test.tsx`, `system-info-api.test.ts`, `App.redemptions.test.tsx`, and `lib/router.test.ts` cover rendering, exact bounded transports, malformed/oversized rejection, cleanup, failure redaction, retry/isolation, dedicated routing, and the root/ordinary-admin boundary. The backend records bounded runtime/host/resource metadata through a monotonic cross-dialect heartbeat upsert. Before that upsert, each heartbeat can delete only its own legacy row when a statement-time database-clock check finds it more than 90 seconds in the future; the guarded repair preserves concurrently newer legitimate rows. Service and model tests cover both paths alongside the existing endpoint authorization/lifecycle contracts. |
| 38 | /system-settings (layout) | Route layout: system settings; redirects to /403 unless SUPER_ADMIN \| auth: admin (SUPER_ADMIN only) | PASS | The root-only guard runs before category normalization, ordinary administrators render 403 without fetching settings, and the feature-scoped editor provides nested category/section navigation with accessible current-page state. Router, App integration, and `SystemSettingsEditor.test.tsx` cover authorization, canonical redirects, selected-section rendering, and navigation. |
| 39 | /system-settings | Route: system settings landing (feature domain: system-settings) \| auth: admin (SUPER_ADMIN only) | PASS | After the root-only guard, the landing performs the reference-compatible replacement redirect to `/system-settings/site`; route tests cover the exact decision and the subsequent category normalization. |
| 40 | /system-settings/auth | Route: system settings - auth page (feature domain: system-settings/auth) \| auth: admin (SUPER_ADMIN only) | PASS | After the root-only guard, the category landing replacement-redirects to `/system-settings/auth/basic-auth`. The shared section registry test covers every valid default/invalid path and the rendered app test proves a lower-role operator reaches 403 before category normalization. |
| 41 | /system-settings/auth/$section | Route: system settings - auth section (feature domain: system-settings/auth) \| auth: admin (SUPER_ADMIN only) | PASS | The exact five-section registry validates and canonicalizes the parameter, and `SystemSettingsEditor` consumes it to render only the selected section while preserving root authorization. Router, registry, and rendered editor tests cover normalization, selection, and navigation; content completeness remains tracked separately in row 60. |
| 42 | /system-settings/billing | Route: system settings - billing page (feature domain: system-settings/billing) \| auth: admin (SUPER_ADMIN only) | PASS | After the root-only guard, the category landing replacement-redirects to `/system-settings/billing/quota`. `router.test.ts` verifies the catalog default and invalid-section repair; `App.redemptions.test.tsx` boots a root user at the category URL and observes the exact final browser path and settings tab. |
| 43 | /system-settings/billing/$section | Route: system settings - billing section (feature domain: system-settings/billing) \| auth: admin (SUPER_ADMIN only) | PASS | The exact six-section registry validates the parameter and drives a section-specific editor. Rendered operations tests separately select payment and model-pricing and prove their controls do not depend on a generic landing page; incomplete settings breadth remains tracked in row 61. |
| 44 | /system-settings/content | Route: system settings - content page (feature domain: system-settings/content) \| auth: admin (SUPER_ADMIN only) | PASS | After the root-only guard, the category landing replacement-redirects to `/system-settings/content/dashboard`; deterministic route tests pin the exact default and prevent invalid sections from bypassing normalization. |
| 45 | /system-settings/content/$section | Route: system settings - content section (feature domain: system-settings/content) \| auth: admin (SUPER_ADMIN only) | PASS | The exact seven-section registry validates, canonicalizes, and renders the selected section, including an explicit empty state where no target options exist. Router and settings-registry tests pin all names/defaults; content breadth remains tracked in row 62. |
| 46 | /system-settings/models | Route: system settings - models page (feature domain: system-settings/models) \| auth: admin (SUPER_ADMIN only) | PASS | After the root-only guard, the category landing replacement-redirects to `/system-settings/models/global`; deterministic tests pin the category/default catalog and the legacy operations/monitoring redirect to models/routing-reliability. |
| 47 | /system-settings/models/$section | Route: system settings - models section (feature domain: system-settings/models) \| auth: admin (SUPER_ADMIN only) | PASS | The exact seven-section registry validates and renders the selected section. The rendered operations suite selects `models/channel-affinity` to prove route-scoped supplemental behavior, while router and registry tests cover every valid/default/invalid path; provider and deployment breadth remains tracked in row 63. |
| 48 | /system-settings/operations | Route: system settings - operations page (feature domain: system-settings/operations) \| auth: admin (SUPER_ADMIN only) | PASS | After the root-only guard, the category landing replacement-redirects to `/system-settings/operations/behavior`; tests pin its exact default plus the reference-compatible legacy monitoring redirect. |
| 49 | /system-settings/operations/$section | Route: system settings - operations section (feature domain: system-settings/operations) \| auth: admin (SUPER_ADMIN only) | PASS | The exact seven-section registry validates and renders the selected section, with canonical repair for invalid input and compatibility redirects for legacy monitoring paths. Router, registry, and editor tests cover the route contract; form breadth remains tracked in row 64. |
| 50 | /system-settings/security | Route: system settings - security page (feature domain: system-settings/security) \| auth: admin (SUPER_ADMIN only) | PASS | After the root-only guard, the category landing replacement-redirects to `/system-settings/security/rate-limit`; deterministic tests pin the exact category default and invalid-section repair. |
| 51 | /system-settings/security/$section | Route: system settings - security section (feature domain: system-settings/security) \| auth: admin (SUPER_ADMIN only) | PASS | The exact four-section registry validates and renders the selected section behind the root guard. Router and settings-registry tests pin valid/default/invalid behavior and the editor exposes typed bounded controls or an explicit empty state; workflow breadth remains tracked in row 65. |
| 52 | /system-settings/site | Route: system settings - site page (feature domain: system-settings/site) \| auth: admin (SUPER_ADMIN only) | PASS | After the root-only guard, the category landing replacement-redirects to `/system-settings/site/system-info`; the base landing first routes to `/system-settings/site`, matching the reference hierarchy. Deterministic tests cover both hops and malformed paths. |
| 53 | /system-settings/site/$section | Route: system settings - site section (feature domain: system-settings/site) \| auth: admin (SUPER_ADMIN only) | PASS | The exact four-section registry validates and renders the selected section, with `site/system-info` as the canonical default and the only bounded generic-option fallback. Router, registry, and rendered editor tests cover the contract; workflow breadth remains tracked in row 66. |
| 54 | /usage-logs | Route: usage logs (redirects to default section) (feature domain: usage-logs) \| auth: authenticated | PASS | After the authenticated guard, the landing replacement-redirects to the exact reference default `/usage-logs/common`. The deterministic router suite pins anonymous return handling and valid/invalid three-section behavior. |
| 55 | /usage-logs/$section | Route: usage logs section (feature domain: usage-logs) \| auth: authenticated | PASS | The exact `common`, `drawing`, and `task` registry drives `UsageLogsView` for both self and administrator scopes. It implements section-specific bounded endpoints and filters, paging, statistics, role-sensitive columns, local sensitive-value masking, common detail breakdown, drawing prompt preview, task failure detail, and same-origin gateway-owned content links. Twenty API/component tests plus two App tests cover exact navigation/transport, owner scope, unsafe URLs, malformed/credential-bearing responses, loading/empty/retry states, serialized loads, cancellation, and stale results. |
| 56 | /users | Route: user management (feature domain: users) \| auth: admin (role >= ADMIN) | PASS | `/users` is admin-guarded and implements bounded list/search/group/role/status filters, whitelisted sorting (including creation and last-login lifecycle times), pagination, role-limited creation/editing, atomic quota controls, status/role actions, confirmed deletion, and factor resets. Root-only permission matrices, built-in/custom identity unbinding, and per-user subscription grant/reset/invalidate/delete dialogs are integrated with fresh role checks. Twenty-eight API/component tests cover exact payloads, credential rejection, confirmations, authorization, retry/empty states, mutation serialization, and unmount safety. |
| 57 | /wallet | Route: wallet (feature domain: wallet) \| auth: authenticated | PASS | `features/wallet/WalletView.tsx` provides the complete bounded balance/check-in/top-up/payment/order/affiliate/subscription workflow. It exposes an absolute `/sign-up?aff=` referral link with copy feedback; converts affiliate input between server-declared USD, CNY, and token display units using validated immutable quota metadata while posting only an exact safe-integer quota; preserves the saved billing preference while presenting a wallet-first fallback when no subscription is active; and explains plan purchase counts/caps, balance cost and affordability, hosted alternatives, lifecycle/reset/quota/group/source/overflow state, remaining days, and history. Order history supports 10/20/50/100 rows, range text, friendly providers, and trade/user copying; payment options expose their exact provider minimums. Epay wallet/subscription results retain the complete bounded signature field set and render an explicit native POST form, while hosted providers remain safe GET links. Thirty-one focused API/component tests cover these contracts, malformed metadata, transports, race-free default amounts, serialization, redaction, ownership, retry, unmount, logout, and Turnstile lifecycle; the focused backend top-up metadata contract also passes. |
| 58 | __root | Route: root layout (TanStack Router root) \| auth: public | PASS | The independently designed root shell supplies pathname/history navigation, setup/auth/role decisions, saved seven-locale preference, status-driven branding, a redacting React error boundary, and the shared authenticated layout. Global sidebar policy is intersected with the user's bounded personal override and live role permissions; malformed or stale settings fail safely. App, layout, navigation, error-boundary, and router tests cover responsive keyboard navigation, active state, feature selection, history replacement, setup/session transitions, logout, and all guards. |
| 59 | (auth) layout | Route layout: auth pages (pathless group) \| auth: public | PASS | `features/auth/AuthLayout.tsx` provides the shared accessible authentication shell and status-driven `SystemBrand`; it handles pending branding, configured name/logo, safe artwork failure, and the public language control without duplicating the authenticated navigation. Dedicated layout and App tests cover loading, configured, and fallback behavior across auth routes. |
| 60 | System Settings: Auth | Settings page (feature: system-settings/auth); sections: basic-auth, oauth, passkey, bot-protection, custom-oauth | PASS | The typed editor covers registration/password/email verification, exact-domain and alias policy, GitHub/Discord/OIDC/Linux DO/WeChat/Telegram built-ins, full relying-party/passkey policy, Turnstile, callback guidance, and database-backed custom-OAuth discovery and bounded CRUD. Option-backed credentials are write-only, complete provider configuration fails closed before advertisement, and a complete deployment environment configuration may override the corresponding option set. Catalog, validation, option-transport, custom-OAuth, status, callback, and rendered authentication tests cover every reachable target workflow. |
| 61 | System Settings: Billing | Settings page (feature: system-settings/billing); sections: quota, currency, model-pricing, group-pricing, payment, checkin | PASS | Typed sections cover canonical new-user quota with legacy fallback, pre-consumption and referral rewards, the immutable 500,000-per-USD accounting scale, USD/token/custom display units, complete model/group/tool/free-model pricing, registration group directives, Epay/Stripe/Creem/Waffo configuration, check-in, compliance, pricing reset, and root-only Waffo Pancake catalog/pairing. All mutable target contracts have bounded validation, confirmation, write-only secret handling, and rendered tests. Stripe API/webhook secrets remain deployment-only and quota scale remains fixed by design; these 12-factor/accounting invariants leave no reachable billing settings workflow incomplete. |
| 62 | System Settings: Content | Settings page (feature: system-settings/content); sections: dashboard, announcements, api-info, faq, uptime-kuma, chat, drawing | PASS | The content domain now has one atomic, validated runtime snapshot for dashboard export policy, site content, announcements, API information, FAQ, chat launchers, drawing forwarding, and Uptime Kuma groups. Disabled API/FAQ/announcement content is omitted from `/api/status`, and disabled uptime produces an empty response without outbound traffic. Uptime aggregation enforces a 12-second aggregate deadline, five-second requests, SSRF-safe dialing with proxy/redirect suppression, decompressed and encoded response caps, bounded groups/monitors, sibling cancellation, and isolated group failures. The typed settings UI provides bounded JSON editors and coherent enablement controls; focused Go and frontend suites cover option updates, status publication, malformed state, disabled behavior, and safe aggregation across all seven localized interfaces. The stricter HTTPS/private-address rule under `SSRF_DISABLE` is an intentional transport invariant, not missing content functionality. |
| 63 | System Settings: Models | Settings page (feature: system-settings/models); sections: global, routing-reliability, gemini, claude, grok, channel-affinity, model-deployment | PASS | Typed sections cover default grouping, request retry and automatic channel-state policy, scheduled health checks, Gemini safety/version/image/thinking/function compatibility, Claude safe headers/output/thinking policy, Grok violation fees, all six channel-affinity controls with live cache maintenance, and io.net enablement/write-only credentials/connection testing. Format-specific validators reject unsafe headers, invalid model/status maps, and out-of-range values before writes; confirmations identify routing or billing impact. Reference knobs with no target runtime consumer are intentionally not presented and leave no supported model workflow incomplete. |
| 64 | System Settings: Operations | Settings page (feature: system-settings/operations); sections: behavior, alerts, email, worker, logs, performance, update-checker | PASS | Typed behavior, quota-alert, performance-metric, and host-performance controls are complemented by an atomic bounded SMTP panel, durable usage-history cleanup with task progress, bounded local log-file retention, and a version/uptime update checker pinned to TokenRouter's canonical GitHub release source. Purpose-built transports are root-scoped, abortable, redacted, size/count/time bounded, and covered by rendered and parser tests; destructive cleanup requires confirmation. Worker remains an explicit non-editable explanation because TokenRouter deliberately uses bounded SSRF-safe direct transports and has no delegated URL/shared-key trust plane to configure. |
| 65 | System Settings: Security | Settings page (feature: system-settings/security); sections: rate-limit, sensitive-words, ssrf, token-limits | PASS | The typed editor covers live per-user and per-group model-request rate limits, sensitive-word enforcement, and maximum user-token limits with bounded format validation, confirmation, and rendered tests. Web/dashboard throttles remain deployment policy. SSRF is intentionally non-mutable: outbound features use `SafeDialContext`, and only the documented process-wide local-development `SSRF_DISABLE` escape exists. Keeping that invariant below application settings is a stronger target security contract, not a missing reachable workflow. |
| 66 | System Settings: Site | Settings page (feature: system-settings/site); sections: system-info, notice, header-navigation, sidebar-modules | PASS | The typed site editor covers branding, restart-scoped server address validation, footer/About/home/legal content, and bounded Notice content. Purpose-built public-header and administrator-sidebar editors expose every supported module as structured controls, preserve safe unknown fields, restore defaults, confirm globally disruptive changes, and reject malformed, oversized, prototype-bearing, or aborted input. The public home and authenticated shell apply the resulting brand and global navigation policy together with saved personal sidebar preferences. Catalog, editor, navigation-panel, application-layout, and shell tests exercise the complete observable site workflow. |
| 67 | NOTE: no web/src/pages/Setting/ | The directory web/src/pages/Setting/ does not exist in the reference; settings live under feature-scoped system-settings modules. | PASS | Target likewise avoids a legacy pages directory: `features/system-settings/SystemSettingsEditor.tsx` owns the bounded typed editor, section-aware navigation, secret non-redisplay, validation, save states, supplements, and rendered tests, while `lib/settings-groups.ts` supplies the exact independently tested category/section registry. |
| 68 | feature domain: about | Top-level frontend feature domain under reference `web/src/features/` | PASS | The cohesive `features/public-documents` module owns About retrieval, content classification, safe rendering, and rendered tests; it is shared intentionally with the two related public legal documents rather than duplicating infrastructure. |
| 69 | feature domain: auth | Top-level frontend feature domain (sign-in, sign-up, forgot/reset, otp, passkey, oauth) | PASS | `features/auth`, `LoginView`, and the dedicated auth layout cover password registration/login/reset, verified email, Turnstile, legal consent, built-in/custom OAuth, WeChat, Telegram, passkey, 2FA/TOTP/backup codes, status-driven policy, and safe return/callback handling. The shared authenticated client additionally recovers an expired access cookie through one credentialed, single-flight refresh and invalidates identity only on terminal failure. API, component, route, layout, and session-refresh tests exercise exact contracts, every advertised provider branch, malformed data, single-use tokens, browser capability failures, cancellation, transient recovery, and redacted errors. |
| 70 | feature domain: channels | Top-level frontend feature domain | PASS | `features/channels` owns a complete independently designed administration surface and strict API boundary: granular capabilities, search/filter/sort/pagination, fresh masked details, complete normal/sensitive editor fields, test/discovery, all bulk/tag/copy/balance/update/repair operations, provider-specific tools, root creation/deletion, and scoped step-up key reveal. Ordinary responses cannot retain credentials, writes are allowlisted, unknown provider JSON is preserved safely, destructive actions are confirmed, and stale work aborts. Fifty-one focused parser/transport/rendered tests cover the domain. |
| 71 | feature domain: chat | Top-level frontend feature domain | PASS | The isolated `features/chat` module implements the reference's configured launcher domain independently of the separate stateless playground: bounded catalog/status transport, safe URL/client-config conversion, caller-owned active-key resolution, direct and first-Web routes, sandboxed rendering, explicit external launch, localized state UX, responsive styles, and deterministic API/component/App tests. Backend setting/status tests independently enforce the compatible public catalog shape and reject unsafe updates atomically. |
| 72 | feature domain: dashboard | Top-level frontend feature domain | PASS | `features/dashboard` owns the four reference sections, strict role-shaped API parsing, canonical query state, account/admin scoping, aggregate and timeline math, model/user distributions, flow paths, local overview sources, performance analytics, accessible dependency-free charts/tables, persistent presentation preferences, retry, and cancellation. Thirty-seven API/component tests plus App routing tests cover the complete domain. |
| 73 | feature domain: errors | Top-level frontend feature domain | PASS | `features/errors` owns all five localized error surfaces, bounded detail, authenticated-name mapping, safe actions, bootstrap-failure coverage, and the React error-boundary regression tests. Route/guard policy and rendered behavior are both verified. |
| 74 | feature domain: home | Top-level frontend feature domain | PASS | `features/home` owns the localized, accessible public landing experience: status-driven brand and complete navigation policy, rich default sections, catalog-backed API examples, independently resilient pricing/performance/rankings/About/legal resources, and combined Notice/announcement UI with safe multiline Markdown and persistent unread state. Operator content may replace the default body without replacing the shared header/notifications; its bounded cache lifecycle and Markdown/isolated-HTML/sandboxed-external-frame renderer include exact-origin theme/language synchronization. Feature, parser, and App integration tests cover success, loading, empty, refresh fallback, redacted failure, unmount, module policy, signed-in/anonymous actions, status wiring, read state, and unsafe-content rejection. |
| 75 | feature domain: keys | Top-level frontend feature domain | PASS | `features/keys` owns a complete secret-safe relay-token API boundary and management surface rather than embedding token state in `ConsoleView.tsx`. Responses and inputs have byte/count/string/integer bounds; list keys must match the backend mask format; failure details are never rendered; requests abort on unmount; write payloads never contain credentials; status-only updates and owner-scoped deletion routes are exact. Component tests cover list/search/pagination, complete create/edit policy data, group-specific model loading, status, single/batch deletion confirmation, loading/error/empty/validation states, and late responses; all user-visible feature copy exists in seven locales. |
| 76 | feature domain: legal | Top-level frontend feature domain (privacy policy, user agreement) | PASS | `features/public-documents` provides the two legal workflows as a bounded, independently tested feature domain, including accessible loading/error/empty states, safe rich content, isolated HTML, and explicit external-document links. |
| 77 | feature domain: models | Top-level frontend feature domain | PASS | `features/models` owns model/vendor metadata and io.net deployment workflows instead of relying on the legacy admin console. Its strict API layer and rendered surface cover both route sections, complete mutations and provider actions, write-only settings, bounds, role gates, confirmation, independent resource errors, retries, serialization, and cancellation; 26 focused tests plus App integration pass. |
| 78 | feature domain: performance-metrics | Top-level frontend feature domain | PASS | `features/performance-metrics` is a reusable bounded domain for exact detail and summary transports, schema-pinned parsing, compact accessible summary badges, group selection, TTFT/latency/success/throughput statistics, sparklines, and a comparison table; pricing uses its summary and detail components directly. Nine parser/transport/rendered tests cover malformed, duplicate, cross-model, unsorted and out-of-range data, exact query bounds, all series, safe labels, retry/empty/redacted-error states, and abort/stale-response safety. |
| 79 | feature domain: playground | Top-level frontend feature domain | PASS | `features/playground` owns a session-only, caller-scoped conversation UI and strict relay adapter. It covers group/model options, multi-turn and system context, advanced generation parameters, streaming reasoning/content, UTF-8 event framing, stop/abort, feature policy, all resource states, redaction, and lifecycle safety in 18 focused tests plus App integration. |
| 80 | feature domain: pricing | Top-level frontend feature domain | PASS | `features/pricing` owns strict catalog/query parsing, exact bounded transport, combined filtering and modality derivation, canonical URL state, and safe tiered-expression inspection; `PricingView` composes the responsive list and target-supported detail workflow with the reusable performance domain. Nineteen pricing parser/transport/rendered tests cover malformed and oversized contracts, dynamic-billing pairing, query/filter behavior, card/table/pagination presentation, exact price/group/route details, all UI states, redaction, and cancellation. |
| 81 | feature domain: profile | Top-level frontend feature domain | PASS | `features/profile` owns the complete account, interface-preference, notification/privacy, verified-email, access-token, 2FA, passkey, session, identity-binding, and account-deletion surface. Login-session parsing uses a bounded Unix-seconds application model with explicit legacy-ISO normalization; the rendered list labels the current session and preserves the existing confirmed revoke/revoke-others lifecycle without rendering full identifiers. Its strict API/WebAuthn adapters and component tests enforce same-origin bounded transports, scoped proofs, write-only secrets, role restrictions, confirmation, per-resource recovery, mutation serialization, cancellation, and redaction across 41 focused tests. |
| 82 | feature domain: rankings | Top-level frontend feature domain | PASS | `features/rankings` now owns the typed snapshot model, strict bounded API adapter, period parsing, responsive rendered view, histories, leaderboards, movement panels and focused route/component/API tests. The feature accepts only the pinned reference's four periods, rejects malformed or oversized data before rendering, never exposes backend failure detail, cancels stale loads, and links model/vendor rows into pricing. Its production endpoint computes current/previous token ranks, vendor aggregation and bounded bucket histories across SQLite/MySQL/PostgreSQL expressions while preserving the legacy flat consumer contract. |
| 83 | feature domain: redemption-codes | Top-level frontend feature domain | PASS | `features/redemptions` owns a strict API boundary and independently designed operator workflow: bounded pagination and name/ID/status search (including virtual expiry), batch creation, current-record reload before safe edit, enable/disable rules, single and invalid-code deletion confirmations, explicit code copying, and all loading/error/empty/pagination states. List responses discard raw 32-hex values immediately and retain only masks; a full existing code is fetched only for an explicit clipboard action and is never rendered, while newly created codes use a clearly labelled one-time panel that auto-hides after 60 seconds. Exact-schema/count/string/integer/response-size validation, allowlisted writes, generic failures, abortable loads, a non-admin no-request gate, and 14 API/component tests substantiate the feature; all copy is translated in seven locales. |
| 84 | feature domain: setup | Top-level frontend feature domain (setup wizard) | PASS | `features/setup` owns a typed, bounded status parser, exact conditional payload builder, and independently designed four-step wizard. Rendered tests cover database presentation, new/existing-root branches, credential validation and retention, persisted mode preselection, review, back navigation, exact requests, duplicate-submit locking, generic failures, and completion handoff; pure tests cover legacy/reference response aliases and malformed-data rejection. The backend exposes database/root/mode state and persists the selected mode atomically with initialization. |
| 85 | feature domain: subscriptions | Top-level frontend feature domain | PASS | `features/subscriptions` owns the administrator plan and user-lifecycle surface: list/filter/page, create/edit/status, compliance gating, plan-wide and per-user reset semantics, grants, invalidation, hard deletion, and root-only Waffo Pancake product binding. Strict API and component tests cover decimal fidelity, allowlisted writes, ownership, roles, confirmations, serialization, cancellation, and all states; self purchase and billing preference are integrated in the wallet domain. |
| 86 | feature domain: system-info | Top-level frontend feature domain | PASS | `features/system-info` owns a strict 512 KiB API boundary and the complete root operational view. It caps node/task counts and every rendered string/number, selects only safe known metadata, rejects invalid statuses/progress/envelopes, encodes deletion identities, and never renders request failure detail. Component tests exercise rich data, active/history grouping, independent empty/failure/retry states, redaction, confirmation and refresh behavior; API and route tests prove exact endpoints and root-only reachability. All visible copy is localized across seven equal-key locales. |
| 87 | feature domain: system-settings | Top-level frontend feature domain (admin settings; subdomains: auth, billing, content, general, integrations, maintenance, models, operations, request-limits, security, site) | PASS | `features/system-settings` owns the root-only, route-aware editor with an exact seven-category/40-section registry, safe secret handling, format-specific JSON/text/number validation, confirmations, per-option saves, cancellation, redacted failures, and purpose-built panels for navigation, custom OAuth, payment compliance, Waffo Pancake, pricing reset, affinity maintenance, io.net connectivity, SMTP, log maintenance, and update checking. Catalog, API, validation, rendered, App-routing, authorization, and all-seven-locale evidence cover every supported target workflow. Only immutable SSRF enforcement and the deliberately absent worker trust plane render explanatory non-editable states. |
| 88 | feature domain: usage-logs | Top-level frontend feature domain | PASS | `features/usage-logs` owns common, drawing, and task history for self and administrator scopes. The bounded API and rendered workflow implement section-specific filters, paging/statistics, role-safe fields, local sensitive-data toggles, detail/preview surfaces, safe gateway content paths, retry, serialization, and cancellation; 20 focused tests plus App routing coverage pass. |
| 89 | feature domain: users | Top-level frontend feature domain | PASS | `features/users` owns the core operator workflow and strict API boundary: both local/reference pagination envelopes, list/search/filter/sort, configured-group suggestions with manual fallback, role-aware creation and management, safe group/remark edits, atomic quota updates, status/role actions, confirmed deletion, and passkey/2FA resets. Credential-bearing, malformed, oversized, or out-of-range responses fail closed; write payloads are allowlisted; passwords are cleared after success or failure; non-admins never issue privileged reads; late responses are ignored. Twenty-eight focused parser/transport/rendered tests cover routes, payloads, authorization, all data states, confirmations, redacted failures, and lifecycle safety, and all seven locales contain the feature copy. |
| 90 | feature domain: wallet | Top-level frontend feature domain | PASS | `features/wallet` owns a strict bounded API boundary and complete responsive, localized workflow for balances, Turnstile check-in, historical redemption, every configured top-up/subscription provider, rolling order history, affiliate credit/referrals, subscription preference, balance purchase, eligibility, and lifecycle/history detail. Server-declared display metadata drives exact USD/CNY/token conversion without trusting client scale, and invalid rate/scale state fails closed. Provider-specific minimums, plan caps/costs/remaining availability, inactive-subscription preference fallback, 10/20/50/100 history paging, range labels, friendly provider names, and copy controls are rendered and tested. Signed Epay stays an explicit native POST, hosted checkouts stay safe GET links, mutations serialize, failures are redacted, and initial payment defaults cannot race with user input. Thirty-one focused API/component tests plus the backend metadata contract substantiate the domain. |
| 91 | locale: en | Reference locale has 5,266 keys under `translation` | PASS | `en.json` has the complete 2,644-key independently designed target catalog and covers all 2,536 runtime keys found by the source scan. A mechanical set comparison records 435 shared, 4,831 reference-only, and 2,209 target-only English keys; importing unused reference implementation copy would not add a reachable target behavior, so exact key-name parity is not an open workflow. |
| 92 | locale: fr | Reference locale has 5,266 keys under `translation` | PASS | `fr.json` has the same complete 2,644-key target set with meaningful translations, zero copied-English violations, and full interpolation parity. It covers all 2,536 runtime keys; reference-only implementation copy has no target call site and is not required for observable parity. |
| 93 | locale: ja | Reference locale has 5,266 keys under `translation` | PASS | `ja.json` has the same complete 2,644-key target set with meaningful translations, zero copied-English violations, and full interpolation parity. It covers all 2,536 runtime keys; reference-only implementation copy has no target call site and is not required for observable parity. |
| 94 | locale: ru | Reference locale has 5,266 keys under `translation` | PASS | `ru.json` has the same complete 2,644-key target set with meaningful translations, zero copied-English violations, and full interpolation parity. It covers all 2,536 runtime keys; reference-only implementation copy has no target call site and is not required for observable parity. |
| 95 | locale: vi | Reference locale has 5,266 keys under `translation` | PASS | `vi.json` has the same complete 2,644-key target set with meaningful translations, zero copied-English violations, and full interpolation parity. It covers all 2,536 runtime keys; reference-only implementation copy has no target call site and is not required for observable parity. |
| 96 | locale: zh-TW | Reference locale has 5,266 keys under `translation` | PASS | `zh-TW.json` has the same complete 2,644-key target set with meaningful translations, zero copied-English violations, and full interpolation parity. It covers all 2,536 runtime keys; reference-only implementation copy has no target call site and is not required for observable parity. |
| 97 | locale: zh | Reference locale has 5,266 keys under `translation` | PASS | `zh.json` has the same complete 2,644-key target set with meaningful translations, zero copied-English violations, and full interpolation parity. It covers all 2,536 runtime keys; reference-only implementation copy has no target call site and is not required for observable parity. |
| 98 | _reports/_sync-report.json | Non-locale artifact under reference locales (i18n sync report), excluded from translation-key counts | PASS | The deterministic report records 2,644 keys for every locale and zero duplicates, missing, extras, invalid values, untranslated values, or interpolation mismatches. The checker scans 110 source files and verifies 2,536 runtime keys, including 2,019 direct literals and 517 keys resolved through catalogs, imports, iteration helpers, and static branches. Its self-test and live validation pass. |
| 99 | TokenRouter payment-compliance control | Root-only settings workflow: current v1 status/timestamp, explicit acknowledgement checkbox, dedicated session-auth confirmation action; compliance metadata excluded from generic editing and ordinary admins never fetch/root-render options | PASS | `PaymentCompliancePanel` is reachable only inside the root-only options tab, requires an explicit checkbox, posts the exact confirmation body, accepts only a bounded current-v1 response, refreshes persisted options, and emits a stable redacted failure. `AdminConsole.operations.test.tsx` proves an ordinary administrator neither fetches root options nor renders the panel, disabled/accepted states, exact request, refresh, success, malformed-response rejection, and error redaction. Backend authorization/session tests independently prove the mutation gate. |
| 100 | TokenRouter model-pricing reset control | Root-only settings action describes the immediate live-billing effect, requires explicit browser confirmation, disables while running, reports success/failure, and refreshes persisted options after completion | PASS | `PricingResetPanel` describes the live-billing effect, requires the exact browser confirmation, disables during the request, posts `/option/rest_model_ratio`, refreshes options, and uses stable success/failure text. The rendered operations suite covers confirmation, exact transport, refresh, success, redacted failure, and the parent root-only authorization boundary; all frontend gates pass. |
| 101 | TokenRouter model performance workflow | Public home view fetches the reference performance summary and renders 24-hour average latency, success rate and token throughput per model; root grouped settings expose enabled/flush/bucket/retention controls | PASS | `features/home/HomeView.tsx` requests `/perf-metrics/summary` with `hours: 24`, validates finite rows, caps display at 20, and renders localized accessible loading/empty/redacted-error/data states; its rendered test verifies latency, two-decimal success/TPS output, endpoint parameters, and late-unmount safety. `settings-groups.test.ts` asserts enabled, flush interval, bucket time, and retention keys. Full frontend test/typecheck/lint/build and translation checks pass. |
| 102 | TokenRouter channel-affinity operations workflow | Root grouped settings expose all six affinity controls; live cache card shows status/counts and confirms per-rule/all clears with busy/success/error states | PASS | `settings-groups.test.ts` asserts the complete six-key configuration set. The live panel strictly bounds and reconciles capacity/unknown/per-rule statistics, rejects malformed clear results, never renders backend details, confirms both per-rule and all-entry deletion, disables unavailable operations, refreshes after success, and reports stable outcomes. `AdminConsole.operations.test.tsx` covers root authorization, valid/malformed reads, both exact delete query shapes, confirmation, success, mutation corruption, and redaction. |
| 103 | TokenRouter prefill-group management workflow | Admin console lists/type-filters groups, creates/edits model/tag lists or endpoint JSON, confirms deletion, supports cancel-edit, and exposes busy/success/error/empty states | PASS | `PrefillGroupPanel` validates bounded group/count/string/item/JSON response and input shapes, safely converts model/tag text lists, validates endpoint JSON, supports filter reload, create/edit/cancel and confirmed deletion, disables writes while busy, and returns stable redacted failures. The rendered operations suite proves filtering, list rendering, edit payloads, invalid endpoint rejection, an exact busy create payload, deletion/confirmation, success/empty states, malformed-response rejection, and secret non-disclosure; full tests, translations, typecheck, lint, and production build pass. |
