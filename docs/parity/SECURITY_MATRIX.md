# Security Matrix

Strict security comparison against reference commit ccd535ef8e50cf6e5846a59278c40b7ff59d1b7d (audited 2026-09-05).

> Status values: NOT_STARTED, IN_PROGRESS, PASS, BLOCKED_EXTERNAL, INTENTIONAL_DEVIATION, REFERENCE_PLACEHOLDER
>
> PASS requires an implemented target control plus deterministic executable evidence for the stated boundary. Source inspection, a helper-only test, a static secret scan, or a mocked happy path alone is not enough. Live third-party verification is recorded separately as BLOCKED_EXTERNAL so it does not overstate local evidence.

Strict totals: **57 PASS / 0 IN_PROGRESS / 0 NOT_STARTED / 4 BLOCKED_EXTERNAL / 0 INTENTIONAL_DEVIATION / 0 REFERENCE_PLACEHOLDER (61 rows).**

| # | Security control | Status | Target evidence and remaining gap |
|---|---|---|---|
| 1 | Relay bearer-token authentication and current authorization policy | PASS | middleware/auth.go authenticates the stored token and live owner before publishing the relay context, and recalculates explicit, inherited, and auto-group authorization on every request. TestTokenAuthDynamicallyInheritsCurrentUserGroup, TestTokenAuthRevalidatesExplicitAndAutoGroups, and TestRelayRejectsInvalidToken cover acceptance and fail-closed changes. |
| 2 | Password hashing, verification work, and import policy | PASS | New and upgraded passwords use bcrypt cost 12. Public registration accepts 8-64 valid UTF-8 characters only when the encoded password also fits bcrypt's 72-byte ceiling, so a multibyte overrun is a 400 with no account mutation. Online login accepts only complete bcrypt encodings at costs 4-12. Unknown accounts and malformed or cost-above-12 stored hashes take one cost-12 sentinel comparison; an accepted cost `c` takes its real comparison plus dummy costs `c` through 11, yielding the same aggregate `2^12` bcrypt work class, and a successful cost-below-12 login compare-and-swap upgrades the stored hash without overwriting a concurrent reset. A process-wide one-to-four-slot gate rejects excess work immediately as stable HTTP 429 `AUTH_PASSWORD_VERIFICATION_BUSY`. Legacy import atomically preflights every nonblank password hash while preserving blank OAuth-only accounts; hashes above cost 12 require password reset or offline remediation and are deliberately never evaluated online. TestPasswordHashUsesBoundedTargetCost, TestPasswordBcryptHashValidation, TestPasswordLoginVerificationPlanEqualizesEveryAcceptedCost, TestPasswordLoginUpgradesLowCostHashWithCompareAndSwap, TestPasswordLoginRejectsImmediatelyWhenVerificationCapacityIsFull, TestWritePasswordAuthenticationErrorMapsCapacityToStable429, TestImportLegacyOneAPIPreflightsPasswordHashesBeforeTargetMutation, TestLegacyPasswordHashPreflightAllowsOAuthOnlyAndBoundedBcrypt, and TestRegisterRejectsPasswordBeyondBcryptByteLimit cover generation, work equalization, overload, upgrade, import, byte bounds, and no-mutation behavior. |
| 3 | Session-secret validation, cryptographic separation, and checked secure entropy | PASS | common/session_secret.go rejects absent, weak, or conflicting production secrets and initializes JWT/AES material before runtime startup. Security-sensitive random APIs in common/crypto.go are error-returning, unbiased, and have no predictable fallback; session/refresh credentials, auth flows and OTPs, security proofs, PATs, API tokens, affiliate/collision suffixes, 2FA backup codes, redemption keys, and top-up/payment order identifiers propagate entropy failure before returning or persisting a credential. Best-effort randomness is explicitly restricted to non-secret correlation/display use. TestValidateConfiguredSessionSecret, TestInitializeSessionSecretConfiguration, TestSessionSecretProtectsJWTAndAES, TestSecurityRandomGeneratorsFailClosed, TestSecurityRandomGeneratorBoundsBrokenReader, TestBestEffortRandomIsExplicitlyNonSecretFallback, TestSecurityIssuanceEntropyFailureLeavesNoMutation, and TestTokenCreateEntropyFailureDoesNotPersistCredential cover the primitive plus representative production no-mutation paths. |
| 4 | Access/refresh issuance, rotation, and replay handling | PASS | service/auth.go persists hashed refresh tokens, rotates session version and refresh proof atomically, and applies the configured replay policy. Login and refresh sign credentials from the locked, validated transaction snapshot before commit, so a post-mutation database read outage cannot strand a newly active session or rotated refresh proof. TestLoginAndRefreshRotation, TestRefreshRotationImmediatelyInvalidatesPreviousAccessToken, TestRefreshReplayPolicy, TestConcurrentRefreshAndRevokeCannotLeaveUsableAccessToken, TestCompleteLoginDoesNotDependOnPostCreateDatabaseReads, and TestRefreshRotationDoesNotDependOnPostCASDatabaseReads cover rotation, replay, concurrency, and the post-write availability boundary. |
| 5 | Live-session validation, revocation, and tombstone retention | PASS | Access JWT acceptance is bound to SID, session version, user auth version, expiry, live user status, and the owning user. Authentication-version rotation explicitly revokes non-kept active sessions, preserving tombstones while immediately releasing their active-session quota. TestSessionAccessTokenRejectsRevokedExpiredDisabledAndStaleState, TestSessionRevocationIsOwnerScopedAndRetainsTombstones, TestRevokeOtherSessionsKeepsOnlyCurrentSessionActive, TestRevokeAllSessionsIsUserScopedAndRetainsTombstones, and TestBumpAuthVersionRevokesOtherSessionsAndReleasesActiveQuota cover the boundary. |
| 6 | Session-management endpoint ownership and logout proof | PASS | controller/session.go scopes list/delete/revoke-others to the authenticated user; logout requires the refresh-token proof rather than trusting a caller-supplied SID. TestLoginSessionEndpointsEnforceOwnership, TestLogoutCannotRevokeArbitrarySID, TestLogoutRequiresRefreshTokenProof, and TestLogoutPropagatesDatabaseFailure provide route and failure evidence. |
| 7 | Browser-session versus management-PAT privilege boundary | PASS | Dashboard PATs retain reference-compatible token CRUD and admin access, but middleware session identity is required for credential rotation, identity binding, passkey enrollment, session mutation, and account deletion. TestManagementPATCannotRotateItself, TestManagementPATCannotDeleteAccount, TestManagementPATCannotEnrollPasskey, TestExistingIdentityAndSessionMutationsRejectManagementPAT, TestManagementPATRetainsReferenceTokenCRUDContract, and TestRevokedSessionJWTFailsImmediatelyWhilePATRemainsIndependent prove both sides. |
| 8 | Admin/root role guards and Casbin failure behavior | PASS | middleware AdminAuth/RootAuth checks role before advancing the Gin chain and AdminAuth fails closed when the authorization engine errors. TestRoleGuards, TestAdminAuthFailsClosedWhenAuthorizationEngineErrors, TestAuthorizeRoleMatrix, TestAddPolicyPersistsAndEnforces, and TestPermissionEnforcementOnChannelRoutes cover 401/403/success and permission policy. |
| 9 | Relay-token IP allow-list enforcement | PASS | middleware.TokenAuth compares the trusted ClientIP against the token allow-list before group publication. TestTokenAuthEnforcesIPAllowListEndToEnd proves an allowed address reaches the handler and a different address receives 403/ip_not_allowed; proxy trust is covered independently below. |
| 10 | Setup singleton and absence of a default root password | PASS | controller/setup.go requires caller-supplied credentials and claims the singleton setup row transactionally. TestPostSetupConcurrentSingleWinner and TestPostSetupFailureRollsBackSingletonClaim prove a single concurrent winner and rollback after a failed initialization. |
| 11 | Authentication-cookie browser attributes | PASS | controller/user.go sets and clears SID/access/refresh cookies with HttpOnly, configured Secure, explicit SameSite=Lax, consistent Path, and matching expiry behavior. TestAuthCookiesUseExplicitBrowserSecurityAttributes and TestClearedAuthCookiesMatchConfiguredSecurityAttributes are exact header tests. |
| 12 | Global streaming request-body ceiling | PASS | router/router.go installs middleware.RequestBodyLimit at the engine root with a 16 MiB ceiling; it rejects known lengths before route work and wraps chunked bodies with http.MaxBytesReader. TestRequestBodyLimitRejectsKnownOversizeBeforeHandler, TestRequestBodyLimitBoundsChunkedBody, and TestRequestBodyLimitAllowsExactBoundary cover all three cases. |
| 13 | Narrow anonymous request-body ceiling | PASS | AnonymousRequestBodyLimit applies the configured default 512 KiB limit to login, registration, reset, OAuth-state, and webhook routes beneath the global ceiling. TestAnonymousRequestBodyLimitAllowsUnderLimit, TestAnonymousRequestBodyLimitRejectsOversize, TestAnonymousRequestBodyLimitUnreadableBody, and TestAnonymousRequestBodyLimitNegativeFallsBackToDefault cover boundaries and error mapping. |
| 14 | Authenticated dashboard endpoint body limits | PASS | Passkey finish bodies are bounded at 1 MiB, playground groups use the global streaming limit before JSON allocation, and channel updates distinguish oversized bodies. TestReadPasskeyBodyEnforcesExactByteLimit, TestRejectPasskeyBodyMapsOversizeSeparately, TestPlaygroundRequestGroupBodyLimit, TestPlaygroundOversizedRequestUsesPayloadTooLargeStatus, and TestUpdateChannelRejectsOversizedBody provide executable boundaries. |
| 15 | Relay protocol input and model-policy limits | PASS | Ordinary relay, Claude Messages, and Gemini-native bodies are capped at 16 MiB; every supported relay protocol enforces token model policy before dispatch. TestParseRequestBodyLimitBoundary, TestClaudeMessagesInboundBodyLimit, TestGeminiNativeInboundBodyLimit, TestRelayModelLimitRejectsEveryProtocolBeforeDispatch, and TestRelayModelCatalogHonorsTokenLimits cover boundaries and protocol breadth. |
| 16 | Buffered upstream response and error limits | PASS | relay/common defines 16 MiB normal JSON, 64 MiB large JSON, 128 MiB binary, and 16 KiB error caps; provider adaptors reject oversize before partial writes. TestReadUpstreamBodyBoundaryAndReadFailure, TestHandleErrorResponseBoundsAndSanitizesBody, TestOpenAIBufferedResponseBoundaryAndReadFailure, TestClaudeBufferedResponseBoundaryAndReadFailure, TestGeminiEmbeddingResponseUsesLargeJSONLimit, and TestNativeNonStreamResponseReadersRejectOversizeWithoutPartialWrite cover selection and exact boundaries. |
| 17 | Streaming and realtime message limits | PASS | SSE events are capped at 1 MiB. Realtime applies a 4 MiB message cap plus message/response counts and bounded idle/write timeouts, and refunds an oversized upstream frame. TestUpstreamSSEScannerBoundary, TestOpenAIStreamRejectsOversizedEvent, TestNativeConversionStreamsRejectOversizedEvents, and TestRealtimeOversizedUpstreamMessageRefundsAndTerminates cover both streaming forms. |
| 18 | Relay HTTP request and transport deadlines | PASS | relay/controller.go bounds configurable request lifetime and configures positive TLS handshake, response-header, idle, and expect-continue timeouts. TestRelayRequestTimeoutUsesBoundedSafeConfiguration, TestApplyRelayRequestDeadlineBoundsRequestContext, TestRelayDeadlineCancelsUpstreamAndStillRefundsDurably, and TestRelayTransportHasConnectionPhaseDeadlines prove that provider I/O is canceled on deadline while durable quota refund still reaches a terminal state. |
| 19 | Panic recovery without response disclosure | PASS | middleware.Recovery returns only the stable internal-error envelope and request correlation while keeping the panic and stack out of the response. TestRecoveryReturnsStableEnvelopeWithoutPanicOrStack panics with a unique marker and proves neither marker nor stack text is returned. |
| 20 | Runtime-wide secret masking, no-secret logging, and durable audit fallback | PASS | Channel/token/user responses mask stored secrets; process/request/panic/upstream logging is bounded and redacted; provider correlations are fingerprinted. Checked audit writes require the primary DB clock for emitted `created_at` even with a healthy sink, and use it for each outbox lifecycle transition. A stable event ID makes sink reconciliation and identical replay single-effect; event-ID/content collisions fail closed. The primary-DB outbox retains its complete payload only while pending delivery, then atomically replaces it with a SHA-256 receipt. Before serving, an indexed batched startup scrub converts all legacy delivered payloads without touching pending rows; each leased periodic delivery pass independently scrubs at most 500 more legacy delivered rows, even when another delivery fails. Receipt-aware and older workers are not rolling-upgrade compatible, so operators must coordinate or drain old workers as described in Known Deviations item 25. Payload/errors and lease tokens remain hidden and bounded. Existing logger/correlation tests plus TestCheckedAuditWriteFallsBackAndWorkerDeliversExactlyOnce, TestAuditOutboxReconcilesAlreadyDeliveredEventID, TestAuditOutboxEnqueueIsIdempotentButRejectsEventCollision, TestCheckedAuditWriteRequiresPrimaryClockEvenWithHealthySink, TestCheckedAuditWriteOverridesPositiveAndNegativeProcessClockSkew, TestAuditOutboxRefreshesAuthoritativeClockForEachLeaseTransition, TestScrubDeliveredAuditLogPayloadsMigratesLegacyRowsAndPreservesPending, TestPeriodicAuditDeliveryScrubsLegacyPayloadEvenWhenDeliveryFails, TestAuditOutboxFailurePersistsBoundedBackoffWithoutPayload, TestAuditLogOutboxPayloadAndLeaseTokenAreHiddenFromJSON, and the recorded-2026-09-05 MySQL/PostgreSQL TestAuditOutboxExternalDatabaseLeaseAndDelivery gate cover the boundary. |
| 21 | Rate limiting and concurrent admission | PASS | Global API limiting honors its enable flag and defaults to 360 requests per 180 seconds; critical and user-critical limiting share their enable flag and default to 20 requests per 1,200 seconds. Malformed, nonpositive, and duration-overflowing values fall back to safe positive defaults while shared-store failures retain fail-open behavior. TestRateLimitConfigurationMatchesReferenceDefaultsAndOverrides, TestRateLimitConfigurationRejectsUnsafeValues, TestConfiguredRateLimitersEnforceLimitsAndSecondBasedExpiry, TestRateLimitEnableFlagsBypassTheStore, TestRateLimitStoreFailureStillFailsOpen, and TestGlobalRateLimitConcurrentAdmissionDoesNotExceedConfiguredLimit cover configuration, expiry, rejection, bypass, failure, and concurrent admission; the email-verification and model-request limiter suites cover their separate contracts. |
| 22 | Trusted-proxy configuration | PASS | middleware.InitTrustedProxies trusts no forwarded header by default, accepts only configured exact IP/CIDR proxies, and clears prior trust on malformed configuration. TestInitTrustedProxiesIgnoresForwardedHeadersByDefault, TestInitTrustedProxiesHonorsHeadersOnlyFromConfiguredProxy, TestInitTrustedProxiesInvalidConfigurationFailsClosed, and TestInitTrustedProxiesInvalidConfigurationClearsPriorTrust are end-to-end ClientIP tests. |
| 23 | Cookie-origin guard | PASS | Refresh and logout use middleware.OriginGuard in secure-cookie mode, accepting missing/same-origin requests and exact configured trusted origins while rejecting others. TestOriginGuardEnforcesTrustedOrigins and TestOriginGuardDisabledByDefault make the production/local-mode distinction explicit. |
| 24 | Dashboard and relay CORS separation | PASS | DashboardCORS is globally installed but scoped to /api and /pg, allows credentials only for the exact current/configured origin, includes X-Security-Proof, rejects malformed/suffix-confusion origins, and leaves relay/bearer surfaces to non-credentialed RelayCORS. TestDashboardCORSAllowsOnlyExactConfiguredOrigins, TestDashboardCORSRejectsUntrustedAndMalformedOrigins, and TestDashboardCORSDoesNotAffectRelayOrNonBrowserRequests cover the boundary. |
| 25 | SSRF primitive and DNS-rebinding defense | PASS | common.ValidateURL rejects non-HTTP schemes, credentials, unresolved hosts, and any unsafe answer; SafeDialContext resolves once, validates every answer, and dials only the pinned set. TestValidateURLRejectsUnsafeSchemesAndHosts, TestSafeDialContextBlocksLoopback, TestSafeDialContextPinsValidatedResolution, TestSafeDialContextRejectsMixedUnsafeResolutionBeforeDial, and TestSafeDialContextTriesOnlyResolvedAddresses exercise the threat model. |
| 26 | Relay HTTP/WebSocket direct SSRF-safe transport | PASS | relayHTTPClient and wsDialer use common.SafeDialContext and no environment proxy, preventing delegated DNS from bypassing validation; credentialed relay HTTP calls refuse redirects. TestRelayTransportsUseDirectSSRFSafeDialing proves both production transport objects use the exact safe dialer with Proxy unset, TestRelayHTTPClientRefusesCredentialedRedirects proves a 307 never contacts the destination, and common SSRF tests prove dial-time behavior. |
| 27 | Channel health, balance, and model-catalog fetch hardening | PASS | service channel fetchers share direct SafeDialContext transports, refuse credentialed redirects, require a 2xx response, and bound balance/model responses at 1 MiB/8 MiB; model sync builds the same direct transport with configured response limit and cancellation. TestChannelOutboundClientsUseDirectSSRFSafeDialing, TestChannelOutboundClientsRefuseCredentialedRedirects, TestBalanceGETRejectsOversizedResponse, TestBalanceGETAcceptsExactResponseLimit, TestFetchUpstreamModelsRejectsOversizedResponse, and TestModelSyncFetchHonorsCancellation cover wiring and limits. |
| 28 | OAuth client SSRF, credential redirect, and response limits | PASS | Built-in and custom OAuth token/userinfo clients plus custom discovery use direct SafeDialContext transports with Proxy unset; credentialed clients refuse redirects, and responses are bounded. TestBuiltInOAuthClientUsesDirectSSRFSafeTransport, TestBuiltInOAuthClientBlocksUnsafeConfiguredEndpoints, TestBuiltInOAuthClientDoesNotFollowCredentialedRedirects, TestCustomOAuthClientCannotBypassSSRFGuardThroughEnvironmentProxy, TestCustomOAuthDiscoveryCannotBypassSSRFGuardThroughEnvironmentProxy, TestCredentialedOutboundClientsRefuseRedirects, TestOAuthResponsesAreBounded, and TestCustomOAuthResponsesAreBounded cover the boundary. |
| 29 | Turnstile middleware and local verifier hardening | PASS | middleware TurnstileCheck gates configured anonymous flows; the service uses a direct SafeDialContext client, refuses redirects, rejects unsafe overrides, caps the response, and distinguishes disabled, invalid, and verifier failure. TestTurnstileCheckValidToken, TestTurnstileCheckInvalidToken, TestTurnstileCheckVerifyError, TestRegisterTurnstileGate, TestTurnstileClientUsesDirectSSRFSafeTransport, TestCredentialedOutboundClientsRefuseRedirects, TestTurnstileClientBlocksUnsafeOverride, and TestVerifyTurnstileResponseIsBounded provide deterministic mock evidence. |
| 30 | Live Cloudflare Turnstile verification | BLOCKED_EXTERNAL | Local request, failure, redirect, SSRF, and size behavior is covered in row 29. A real siteverify exchange requires a valid Cloudflare site/secret pair and network access, neither of which is available in the audit environment. |
| 31 | TOTP 2FA, backup-code, lockout, and fail-closed state | PASS | service/twofa.go hashes backup codes, atomically consumes them, serializes failure counters, enforces lockout, and rolls factor/session changes back together. The primary database clock drives TOTP validation, backup-code timestamps, last-use/update timestamps, and the exact lockout boundary; a clock-read failure aborts without recording an attempt or authentication. Enable and disable sign the replacement access token from the kept session snapshot inside the same factor/session transaction, so signing or session failure rolls back the factor mutation and an outage immediately after commit cannot strand successful enablement without its recovery codes or access credential. TestTwoFABackupCodeSingleUse, TestConcurrentTwoFAFailuresReachLockoutWithoutLostWrites, TestVerifyTwoFAFailsClosedWhenStateCannotBePersisted, TestEnableTwoFAAndRotateSessionRollsBackFactorOnSessionFailure, TestEnableTwoFAAndRotateSessionWithAccessRollsBackOnSigningFailure, TestDisableTwoFAWithCodeAndRotateSessionWithAccessRollsBackOnSigningFailure, TestEnableTwoFARouteContract, TestEnableTwoFARouteReturnsRecoveryMaterialWhenDatabaseDropsAfterCommit, TestEnableTwoFASessionLossUsesAuthErrorAndRollsBackFactor, TestEnableTwoFAStorageFailureIsInternalAndDoesNotClearCookies, TestDisableTwoFAVerifiesExistingFactorWithoutStateLoss, TestLoginFailsClosedWhenTwoFAStatusCannotBeRead, TestTwoFATOTPUsesAuthoritativeClockDespiteProcessSkew, TestEnableAndDisableTwoFAUseAuthoritativeTOTPClock, TestTwoFALockoutUsesAuthoritativeClockBoundary, and TestTwoFAVerificationFailsClosedWhenAuthoritativeClockFails cover concurrency, rollback, post-commit availability, clock skew, boundaries, controller errors, and storage failure. |
| 32 | Session-bound security-proof step-up tokens | PASS | service/security_proof.go issues five-minute HMAC proofs bound to user, SID, session version, auth version, method, and scopes; sensitive controllers require the precise scope/method. TestVerifySecurityProofSessionBinding, TestVerifySecurityProofScopeAndMethod, TestIssueSecurityProofRejectsIncompleteIdentity, TestUniversalVerify2FA, and TestChannelKeyRootOnlyAndScopeCheck cover binding and authorization. |
| 33 | Passkey registration ceremony session binding | PASS | BeginPasskeyRegistration persists a one-time 5-minute flow bound to the exact user and SID; finish reuses those immutable fields and requires a real browser session, not a PAT. TestWebAuthnInitAndBeginRegistration proves a mismatched SID cannot consume the valid flow, TestManagementPATCannotEnrollPasskey proves the credential boundary, and TestPasskeyRegisterGateWith2FA proves the reference step-up gate. |
| 34 | Passkey uniqueness, replacement, and auth rotation | PASS | model has unique credential-ID and one-active-passkey-per-user indexes; migration purges deleted tombstones and fails closed on ambiguous live rows. Enrollment replacement and current-session auth-version rotation commit in one transaction. TestSecuritySchemaConstraints, TestPrepareSecuritySchemaMigrationPurgesDeletedPasskeyTombstones, TestPrepareSecuritySchemaMigrationRefusesAmbiguousDurableRows, TestReplacePasskeyCredentialAndRotateSessionIsAtomic, and TestReplacePasskeyCredentialAndRotateSessionRollsBackOnSessionFailure cover the constraints and rollback. |
| 35 | Passkey assertion persistence, step-up, deletion, and admin reset | PASS | Assertion updates cannot replace registration identity and fail closed on storage errors/clone warnings; verify flows are SID-bound; user deletion rotates auth and admin reset revokes every session. TestPersistPasskeyUseIsCheckedAndRecordsCloneWarning, TestPersistPasskeyUseFailsClosedOnDatabaseError, TestPasskeyVerifyFinishFlowBinding, TestDeleteAllPasskeysAndRotateSessionIsAtomic, TestResetPasskeysAndRevokeSessionsIsAtomic, TestAdminResetPasskey, and TestAdminResetPasskeyRoleGuard cover the lifecycle. |
| 36 | OAuth state intent, exact-session callback, retry, and replay | PASS | controller/oauth.go validates provider/intent, binds bind-state to user+SID, peeks before transient provider work, requires the callback request to carry that exact live session, then uses an atomic exact-field consume before applying identity. TestGenerateOAuthStateValidation, TestGenerateOAuthStateBindRequiresSession, TestOAuthBindCallbackRequiresExactLiveRequestSession, TestOAuthCallbackProviderFailuresLeaveLoginAndBindFlowsRetryable, and TestConsumeAuthFlowExactConcurrentSingleWinner cover theft, burn, retry, and replay. |
| 37 | Built-in OAuth identity uniqueness and atomic ownership | PASS | model.ExternalIdentityClaim enforces unique provider+canonical-subject and user+provider ownership, including deleted users; migration rejects ambiguous/conflicting legacy rows. Login/bind and admin clearing update the mirrored user field and durable ownership claim in one transaction. TestExternalIdentityClaimsEnforceExactSingleOwnershipForEveryBuiltIn, TestBuiltInExternalIdentityMigrationRejectsAmbiguousLegacyOwners, TestLoginOrBindUserUsesCanonicalClaimsForEveryBuiltInProvider, TestConcurrentBuiltInLoginConvergesOnOneOwner, TestConcurrentBuiltInBindHasOneOwner, TestBuiltInIdentityCreationFailureRollsBackUserAndClaim, TestBuiltInIdentityMirrorFailureRollsBackClaim, and TestClearUserIdentityBindingRollsBackOwnershipRelease cover concurrency and storage failure. |
| 38 | Custom OAuth identity uniqueness and access policy | PASS | model.UserOAuthBinding has unique user+provider and provider+provider-user keys; service evaluates the validated policy document before login/bind. TestUserOAuthBindingLifecycle, TestCustomOAuthProviderValidation, TestAccessPolicyDocumentValidation, TestCustomOAuthFullLoginFlow, TestCustomFetchUserInfoPolicy, and TestCustomOAuthCallbackPolicyDenied cover ownership and policy outcomes. |
| 39 | Verified-email normalization and durable uniqueness | PASS | model.VerifiedEmailKey is a nullable unique SHA-256 key populated only for verified addresses; normalization is bounded, deleted owners retain the claim, and migration aborts on ambiguous legacy ownership. Admin clearing atomically removes the email, verified flag, and durable key for a strictly lower-role target. TestNormalizeVerifiedEmailBounds, TestVerifiedEmailOwnershipAndMigrationFailClosed, TestVerifiedEmailMigrationRejectsAmbiguousLegacyOwners, TestEmailBind, TestEmailBindTaken, TestEmailBindRequiresBrowserSession, and TestAdminClearUserBindingRoute provide schema and route evidence. |
| 40 | Live OAuth provider ceremonies | BLOCKED_EXTERNAL | Built-in/custom mock exchanges, state handling, canonical identity ownership, policy, SSRF, redirects, and response caps are covered locally. End-to-end GitHub/Discord/OIDC/LinuxDO or operator SSO verification requires real registered clients, redirect URIs, secrets, and external network access. |
| 41 | Telegram login and bind verification | PASS | service.VerifyTelegramLogin implements the Telegram HMAC data-check-string and expiry contract; binding uses a one-time session-bound flow. TestVerifyTelegramLogin, TestVerifyTelegramLoginNoToken, and TestTelegramBindFlow cover valid, invalid, missing-secret, and bind behavior. |
| 42 | WeChat exchange, identity handling, SSRF, redirects, and bounds | PASS | service/wechat.go validates configurable bases, uses direct SafeDialContext with no proxy, refuses every redirect because both the server token and one-time code are sensitive, requires a 2xx response, and caps provider responses at 1 MiB; controller handles register/login/bind and disabled/banned states. TestGetWeChatIdByCodeBlocksUnsafeConfiguredDestinations, TestWeChatHTTPClientCannotDelegateResolutionToEnvironmentProxy, TestWeChatHTTPClientRefusesCredentialedRedirects, TestWeChatUserEndpointRejectsAmbiguousBaseURLs, TestGetWeChatIdByCodeResponseIsBounded, TestWeChatAuthRegistersAndLogsIn, and TestWeChatBind cover local behavior. |
| 43 | Live WeChat login verification | BLOCKED_EXTERNAL | Local mocked provider, failure, SSRF, redirect, response-limit, and controller behavior is covered in row 42. A real code-to-openid exchange requires a live WeChat application, secret, callback setup, and network access. |
| 44 | Channel/token key masking and scoped disclosure | PASS | List/search/detail responses mask keys; token disclosure is owner-scoped, rate-limited, POST-only, and batch-bounded, while channel disclosure is root-only with a session-bound 2FA/passkey proof. TestTokenListMaskedAndScoped, TestTokenGetAndKeyDisclosure, TestChannelKeyMaskedAndStepUp, TestChannelKeyRootOnlyAndScopeCheck, and TestAdminUserResponsesNeverExposePasswordHashesOrPATs cover response surfaces. |
| 45 | Admin user mutation isolation and privilege protections | PASS | Generic admin update cannot escalate roles or write corrupt quota/status; quota/profile, built-in/custom OAuth binding, passkey, 2FA, and delete mutations require actor role strictly greater than target role, so even root cannot mutate itself or another root. Creation uses a persistence whitelist with transactional permission provisioning; deletion transactionally revokes sessions, deletes tokens, and soft-deletes the lower-role user. TestGenericAdminUserUpdateCannotEscalateRoles, TestGenericAdminUserUpdateRejectsCorruptQuotaAndStatus, TestAdminManageUserRoleGuards, TestAdminResetPasskeyRoleGuard, TestAdminDisable2FA, TestAdminUserOAuthBindingsContract, TestAdminClearUserBindingRoute, TestAdminDeleteUserRouteAndRoleGuard, TestAdminCreateUserContract, TestAdminCreateUserValidationAndRoleGuards, and TestAdminUserResponsesNeverExposePasswordHashesOrPATs cover self/peer denial and lower-role success. |
| 46 | Root-only compliance, option, and pricing controls | PASS | Option reads redact sensitive values rather than redisplaying stored credentials. Generic writes accept only bounded scalar keys/values, block the compliance keys and immutable quota unit, and route pricing domains through complete validation and atomic publication; compliance mutation, ratio reset, and Waffo Pancake catalog/pair/save/product operations are root-only. Pancake's private key is omitted from reads, blank-key saves can preserve it only for the same merchant, and credentials, binding, unit price, and minimum publish together after validation. Malformed, negative, non-finite, or non-positive pricing values are rejected before persistence/cache publication. TestRootOptionSecurityContract, TestRootOptionUpdateRejectsUnsafeOrStructuredValues, TestRootOptionReadFailsClosedOnUnsafeStoredData, TestOptionAndComplianceRoleGuards, TestResetModelRatioContract, TestResetModelRatioRoleGuard, TestPricingOptionsWriteThroughToLiveBilling, TestSaveWaffoPancakeConfigIsAtomicAndBlankKeyCannotChangeMerchant, and TestWaffoPancakeRootAdministrationContract provide route and live-cache evidence. |
| 47 | Affinity-key privacy and cache-control authorization | PASS | Raw affinity values are SHA-256 keyed and never returned; telemetry uses short fingerprints, cache mutation is root-only, usage is admin-only, and malformed rules fail before publication. Client-header passthrough is restricted to named tracing/session metadata families after rejecting credential-like fragments, authority/forwarding metadata, and hop-by-hop state; it also cannot overwrite a provider header already set on the outbound request. TestRuleBasedChannelAffinityRoutingAndPrivacy, TestChannelAffinityCapacityExpiryAndClear, TestChannelAffinityPassHeadersTemplate, TestChannelAffinityPassHeadersRejectCredentialsAndInvalidNames, TestChannelAffinityPassHeadersCannotOverwriteProviderCredentials, TestChannelAffinityCacheControllerContract, TestChannelAffinityUsageControllerContractAndRoles, and TestChannelAffinityOptionValidation cover privacy, validation, runtime copying, and authorization. |
| 48 | Stripe webhook signature, economic binding, reconciliation, and reversal policy | PASS | The bounded constant-time signature/state gate remains, while fulfillment additionally requires the exact provider mode/type/session/customer/owner/amount/currency snapshot. Pending checkout creation is durable and reconciled through DB-time leases with stale-worker fencing; corrupt bindings and refunds/disputes enter durable manual review rather than guessing. Signature tests plus TestStripeWebhookRequiresDisjointTypeModeAndSessionBindings, TestCompleteStripeTopUpBindsAmountAndCurrencyInsideSettlement, TestStripeCheckoutReconciliationLeaseAllowsOnlyOneProviderCall, TestStripeCheckoutReconciliationStaleLeaseCannotCredit, TestStripeCheckoutCorruptSnapshotMovesToManualReview, and TestStripeRefundAndDisputePolicyOnlyMarksManualReview cover the state machine. |
| 49 | Top-up atomic completion, immutable credit, and idempotency | PASS | Completion locks the stored order/user, validates an immutable versioned server-issued credit and exact provider binding, and commits pending-to-success plus credit/audit in one transaction; legacy unsafe pending rows quarantine and duplicate success is a no-op. The Waffo Pancake path additionally requires a fresh valid provider signature and exact store, environment, order, buyer, product, amount, and currency snapshot; replay is single-effect and durable failure returns a retryable webhook response. TestTopUpCreditSnapshotOverflowAndLegacyPendingFailClosed, TestBoundStripeTopUpRequiresExactSessionAndQuarantinesLegacyPending, TestConcurrentTopUpCompletionCreditsExactlyOnce, TestTopUpCompletionRollsBackWhenDurableAuditCannotBeEnqueued, TestWaffoPancakeWalletOrderBindsEconomicsAndCreditsOnce, and TestWaffoPancakeWalletAndSubscriptionHTTPContractEndToEnd cover atomicity and recovery. |
| 50 | EPay signature/provider/method/amount binding | PASS | controller.EpayNotify accepts only a verified callback, exact stored provider and method, exact bounded decimal amount, and then calls the same idempotent CompleteTopUp transition; unsafe pricing and amounts fail before provider/order mutation. TestTopUpEpayPayAndNotifyContract, TestTopUpMoneyNormalizationAndExactComparison, TestTopUpRequestsFailClosedOnUnsafeAmountsAndPricing, and TestPaymentNumericOptionsRejectUnsafeValues cover valid and forged metadata. |
| 51 | Subscription purchase, immutable entitlement, usage-epoch fencing, expiry, and order completion | PASS | Locked transactions/CAS protect purchase/consumption; each reservation captures a quota-window epoch so delayed exact/partial/overage settlement or refund reaches terminal state without touching current usage. Paid orders carry immutable cadence/group/economic snapshots and capacity reservations; Waffo Pancake uses the plan-specific product and exact signed economics while fulfillment remains bound to the pre-I/O entitlement even if the plan later changes. Expiry drains all due users and recomputes overlapping upgrades. TestUsageEpochFencesDelayedSubscriptionRefundAndSettlement, TestDurableUsageEpochFencesEverySettlementShape, TestSubscriptionResetCadenceSnapshotSurvivesPlanMutationAndDeletion, TestConcurrentStripeSubscriptionReservationsCannotOversubscribeCap, TestWaffoPancakeSubscriptionReservesCapacityAndUsesImmutableEntitlement, TestExpireDueSubscriptionsDrainsUsersAndReconcilesUpgradeChainOnce, and the MySQL/PostgreSQL TestSubscriptionUsageEpochExternalDatabaseLifecycle gate cover the lifecycle. |
| 52 | Live payment gateway verification | BLOCKED_EXTERNAL | Stripe and EPay signatures, webhook states, amount binding, retry semantics, atomic credit, and concurrency are deterministically tested with local fixtures. Real checkout/webhook delivery requires live or sandbox gateway accounts, secrets, products, callbacks, and network access. |
| 53 | Ordinary relay quota reservation and exactly-once settlement | PASS | service.NewRelayQuotaReservation creates wallet/subscription funding, limited-token hold, and durable reservation in one transaction before dispatch; relay then persists dispatch and settles/refunds user+token together. TestSubscriptionTokenRelayReservationRollsBackEveryCreationBoundary, TestOrdinaryRelayReservationPreventsConcurrentTokenOverspend, TestOrdinaryRelayPersistsDispatchedStateBeforeUpstreamCall, TestOrdinaryRelaySettlementFailureRollsBackUserAndTokenTogether, and the MySQL/PostgreSQL TestRelayAccountingExternalDatabaseConcurrency gate cover rollback and concurrency windows. |
| 54 | Realtime quota reservation and terminal accounting | PASS | Realtime reserves before either WebSocket handshake, persists dispatch before upstream dial, charges each unique usage response, refunds unused disconnects, retains ambiguous settlement for recovery, and never double-refunds after a charged log failure. TestRealtimeRejectsQuotaBeforeEitherHandshake, TestRealtimeDispatchMarkerFailurePreventsUpstreamDialAndRefunds, TestRealtimeAccountsEachUniqueResponseAndRefundsDisconnectReservation, TestRealtimeDisconnectBeforeUsageRefundsReservation, TestRealtimeSettlementFailureRetainsReservation, and TestRealtimeLogFailureIsSurfacedAfterChargeWithoutDoubleRefund cover the lifecycle. |
| 55 | Durable relay reservation recovery, lease ownership, and operator review | PASS | The durable ledger persists operation/terminal markers and processing ownership. Recovery retries settle/refund, reclaims expired leases, refunds undispatched holds, and sends accepted ambiguity to bounded manual review. Root-only filtered views and append-only, secret-free audit events allow idempotent retry-to-settle/refund without rewriting economics. Recovery tests plus TestManualReviewRelayQuotaListIsBoundedFilteredAndSecretFree, TestRetryManualReviewSettlementIsAtomicAuditedAndIdempotent, TestRetryManualReviewRefundReturnsExactHoldAndAuditsOnce, and TestRelayQuotaReservationManualReviewRootWorkflow cover both automatic and operator paths. |
| 56 | Distributed system-task lock ownership and renewal | PASS | model/system_task.go transactionally claims a per-type taskID+runner lease, permits stale takeover only through CAS, requires the exact unexpired lease for progress/terminal writes, and releases or expires task+lock atomically. service/system_task.go renews the 60-second lease every TTL/3 and cancels a context-aware handler on lock loss. TestConcurrentSystemTaskClaimHasSingleWinnerAndLock, TestStaleSystemTaskLeaseTakeoverFailsPreviousRunAtomically, TestStaleSystemTaskCleanupUsesLockExpiryNotTaskTimestamp, TestSystemTaskLeaseRejectsWrongOwnerAndExpiredOwner, TestSystemTaskHeartbeatRenewsLease, TestSystemTaskHeartbeatCancelsHandlerOnLockLoss, TestTerminalLeaseReleaseFailureRollsBackTaskState, and TestStaleLeaseCleanupFailureRollsBackLockDeletion cover contention, ownership, heartbeat, and failure windows. |
| 57 | Background maintenance failure ownership and visibility | PASS | The controlled runner returns claim/terminal-write failures, periodic relay reconciliation is deterministically handed to a distributed lease, subscription reset/expiry drains bounded batches while surfacing failures, and channel health atomically commits status plus ability changes. TestPeriodicJobsSchedulesRelayQuotaReconciliationUnderLease, TestRunPendingSystemTasksOnceReturnsTerminalWriteFailure, TestResetDueSubscriptionQuotasReturnsFailuresAndContinues, TestExpireDueSubscriptionsDrainsUsersAndReconcilesUpgradeChainOnce, and TestChannelHealthStatusAndAbilityWriteRollBackTogether cover scheduling, failure visibility, and transaction ownership. |
| 58 | Jimeng credential, outbound-resource, atomic reservation, recovery, and task ownership hardening | PASS | Direct credentials use HMAC-SHA256 and gateways use bearer mode; outbound resources are SSRF-safe/bounded; task fetch is owner-scoped. Funding, token hold, task, and operation journal are created atomically. The primary journal is database-backed with encrypted credentials/provider IDs, DB-time leases and fencing; the encrypted file is secondary emergency evidence. Post-accept ambiguity is never redispatched, terminal secrets are scrubbed, and settlement/audit commit once. TestClientSignsDirectAndGatewayRequests, TestJimengAtomicCreationRollsBackEveryPredispatchBoundary, TestJimengDispatchingRecoverySurvivesRestartWithoutRedispatchOrGuessingCharge, TestJimengWorkerImportsEncryptedEmergencyJournalBeforeUnknown, TestJimengForegroundAndWorkerTransitionsAreFencedByStateAndOwner, TestJimengProviderPayloadPersistenceIsCanonicalAndPrivate, TestJimengSettlementAndAuditOutboxShareOneCommitAndReplayOnce, and the MySQL/PostgreSQL TestJimengExternalDatabaseAtomicLifecycle gate cover the critical windows. |
| 59 | Root performance-maintenance authorization, filesystem confinement, and resource bounds | PASS | All six `/api/performance` routes require RootAuth and disable caching; destructive actions also use CriticalRateLimit. service/performance.go rejects traversal/root/oversized paths and directory symlinks, anchors direct-child operations with `os.Root`, ignores symlinks/non-regular/nested entries, bounds configuration and directory scans, and completes the scan preflight before any deletion. Handler errors expose stable categories rather than local paths. TestPerformanceManagementRoutesRequireRoot, TestPerformanceDiskCacheCleanupIsBoundedAndSymlinkSafe, and TestPerformanceLogMaintenanceRejectsTraversalSymlinksBoundsAndFailures cover authorization, traversal, symlinks, non-recursion, bounds, no-partial-delete, and failure disclosure. |
| 60 | Tool-price configuration, observation, and accounting abuse bounds | PASS | Tool-price JSON is size/cardinality/name/digit/scale/exponent bounded, rejects duplicates and hostile numeric forms before decimal work, publishes atomically as immutable snapshots, and treats explicit zero as terminal. Per-request observation rejects unsafe names, ambiguous identities, excess calls/counts, cross-protocol markers, and retry replays; exact decimal settlement fails closed on invalid group ratios and individual or combined quota overflow without refunding accepted work. TestToolPriceStrictParserRejectsMalformedAndAmbiguousConfiguration, TestToolPriceParserBoundsSizeCardinalityAndCoefficient, TestToolPriceHostileExponentsRejectBeforeDecimalOperations, TestToolPriceAtomicReplacementPreservesOldSnapshotsAndRejectsInvalid, TestToolPricingContextAndModeAreExplicit, TestResponsesToolCallsDeduplicateReplayAndSeparateCustomFromBuiltIn, TestToolObservationAndCountCaps, TestToolObservationRejectsUnsafeNamesAndOversizedReportedCount, TestToolSettlementFailsClosedOnInvalidRatioAndOverflow, TestOrdinaryRelayRejectsToolQuotaClampAfterAcceptedWork, and TestOrdinaryRelayRejectsCombinedModelAndToolQuotaOverflow cover configuration, publication, identity, replay, bounds, and accepted-work behavior. |
| 61 | Root log-cleanup task authorization, time/batch bounds, and durable execution | PASS | `POST /api/system-task/log-cleanup` is RootAuth- and CriticalRateLimit-protected. It rejects missing/future targets, fixes new tasks to a 100-row batch, deduplicates an active cleanup, and exposes only bounded decoded task state through the root system-task routes. The worker revalidates persisted targets and batch sizes before deletion, deletes only logs older than the target in bounded batches, and runs under the fenced system-task lease. Scheduled configured log-sink cleanup accepts `LOG_RETENTION_DAYS` only from 0 through 36500; `0` disables scheduling but never this root-triggered manual path, while a valid horizon whose database-clock cutoff is at or before the Unix epoch succeeds without enqueueing a meaningless task. TestSystemTaskLogCleanupRequiresRoot, TestSystemTaskLogCleanupContract, TestLogCleanupRejectsFutureTargetsBeforeEnqueueOrDeletion, TestLogCleanupRejectsOversizedPersistedBatchBeforeDeletion, TestLogRetentionIsStrictAndInvalidValuesDoNotDelete, TestLogRetentionZeroDisablesAndBoundaryDeletes, TestAutomaticRetentionSharesDurableCleanupQueueWithManualRequests, TestAutomaticRetentionBeforeUnixEpochIsSuccessfulNoOpAtDatabaseClock, TestSystemTaskTransitionsRequireOwningRunner, and TestSystemTaskHeartbeatCancelsHandlerOnLockLoss cover authorization, validation, retention bounds and the pre-epoch no-op, batching, deduplication, ownership, and lock loss. |

## Focused verification commands

Rows 60 and 61 cite exact current-tree tests. Include these expanded focused
selectors in the final current-tree verification gate:

    go test ./common ./middleware ./model ./setting ./service ./controller ./relay ./relay/common ./relay/channel/openai ./relay/channel/claude ./relay/channel/gemini ./relay/channel/jimeng -run 'Session|Auth|Password|Token|Role|Permission|Setup|Cookie|Body|Limit|Response|Realtime|Recovery|Rate|Proxy|Origin|CORS|SSRF|OAuth|Binding|Admin|DeleteUser|Turnstile|TwoFA|Passkey|Email|Telegram|WeChat|Channel|Affinity|Stripe|TopUp|Payment|Subscription|RelayQuota|Jimeng|SystemTask|Performance|ToolPrice|ToolPricing|ToolSettlement|ToolObservation|LogCleanup' -count=1
    go test -race ./common ./middleware ./model ./setting ./service ./controller ./relay -run 'Concurrent|Rotation|OAuthBind|OAuthCallback|Binding|Admin|DeleteUser|Passkey|RelayQuota|Realtime|SystemTask|TopUp|Subscription|SSRF|CORS|Proxy|Recovery|TokenAuthEnforces|StripeWebhook|Performance|ToolPrice|ToolPricing|ToolSettlement|ToolObservation|LogCleanup' -count=1

The final documentation diff is checked with:

    git diff --check -- docs/parity/SECURITY_MATRIX.md
